# arXiv 论文雷达 2026-05-07

## 最值得读

| 序号 | 论文 | 链接 | 关键信息 | 判断 | 适合谁读 | 推荐 |
|---:|---|---|---|---|---|---|
| 1 | LeakDojo: Decoding the Leakage Threats of RAG Systems | [arXiv](https://arxiv.org/abs/2605.05818v1)<br>[PDF](https://arxiv.org/pdf/2605.05818v1)<br>[HTML](https://arxiv.org/html/2605.05818v1) | 问题：RAG systems expose valuable proprietary or sensitive knowledge bases through a black-box query interface. Existing leakage attacks were evaluated under inconsistent setups, usually on simple RAG pipelines, making it hard to know which attacks work, why they work, and how modern RAG modules or stronger LLMs change the risk.<br>方法：LeakDojo models a leakage attack as an anchor-query generator plus an adversarial instruction, and models the target RAG as configurable retrieval, reranking, rewriting, summarization, LLM, and defense modules. The paper benchmarks six existing attacks under a fixed 200-query budget across four datasets, multiple RAG variants, and 14 LLMs. It uses CCL for cumulative leaked unique chunks, SLT for whether the model is induced to leak, ARC for retrieval coverage, and CRR for verbatim recovery quality. The authors also run full-matrix swaps between query generators and adversarial instructions, evaluate defenses, and propose stealthier instruction variants as a case study.<br>实验：Strong. The experiments cover four domains: SciFact, NFcorpus, Enron Email, and FiQA; six primary LLMs plus eight supplementary models; six attacks; several RAG configurations; metric threshold checks; decoding-temperature checks; long-budget PoR behavior; and defense ablations. The setup details include retriever settings, chunking assumptions, MMR vs KNN rationale, model lists, adversarial instruction templates, and metric definitions. The largest caveat is that the paper still relies on selected public corpora and specific synthetic/implemented attack templates, so external validity for private enterprise RAG deployments needs more evidence.<br>不足：The paper explicitly does not fully account for attack cost such as generation latency and token spend, though it adds some cost analysis. It cannot cover the full design space of modern RAG systems and focuses on rewriter, reranker, and summarizer modules. The databases are public or benchmark corpora, which may not match proprietary document structure, access controls, or logging. The defense study is useful but relatively simple: intent detection and ROUGE-L output filtering may not represent mature production defenses. Some conclusions depend on the chosen leakage metric threshold and on model-specific instruction-following behavior. | A strong systematic benchmark for RAG database leakage: it shows leakage is roughly the product of retrieval coverage and instruction-induced verbatim disclosure, and that more faithful RAG systems can become easier to leak. | RAG security researchers / applied LLM security teams / enterprise RAG platform owners / red-teamers | 必读 |
| 2 | One Turn Too Late: Response-Aware Defense Against Hidden Malicious Intent in Multi-Turn Dialogue | [arXiv](https://arxiv.org/abs/2605.05630v1)<br>[PDF](https://arxiv.org/pdf/2605.05630v1)<br>[HTML](https://arxiv.org/html/2605.05630v1) | 问题：多轮攻击会把恶意目标拆成多个看似正常的问题，单轮 guardrail 或只看用户 query 的监控器很容易漏掉；如果过早拦截，又会误伤正常的技术、学术或安全研究对话。论文要解决的是在每一轮候选回复发出前，判断这次回复是否会让累计对话达到足以实施危害的“闭环点”。<br>方法：论文先把防御建模成 response-aware 的 cost-sensitive stopping problem：防御器看到历史、当前用户问题和候选助手回复，选择 PASS 或 BLOCK，目标是在 first harm-enabling turn 精准阻断。为训练和评测，作者构造 MTID：用改造后的 CKA 自适应树搜索生成化学和网络安全领域的多轮攻击轨迹，标注首次危害闭环轮次，并配对术语相近的 benign hard negatives。TurnGate 以 Qwen3-4B 为骨干，先用 turn-level 标签做加权 SFT，再用离线多轮 RL 和过程奖励优化早拦、漏拦、误拦之间的权衡。<br>实验：实验比较充分。离线 MTID 按 seed 级别 70/15/15 切分，包含 8000 条 harmful 和 8000 条 benign 对话，比较了 prompt monitor、Sequential Monitor、Intention Analysis、Llama/Qwen Guard、summary+guard、trajectory-level SFT、turn-level SFT 等基线。主结果显示 TurnGate 在保持 benign pass-through 约 0.834 的同时，把 miss rate 降到 0.177，整体 F1 明显高于 Reweighted-SFT 和各类 guardrail。论文还做了闭环在线评测，让自适应 CKA 攻击者根据实际响应 backtrack/reroute，并做跨 benchmark、跨 target model、跨 attacker pipeline 与化学/网络安全跨域泛化。弱点是若干结论依赖 LLM judge 和 CKA 生成分布，真实用户与真实攻击者覆盖仍有限。<br>不足：闭环轮次本身不是绝对真值，会受攻击者先验知识、judge 模型和 sufficiency 标准影响；MTID 主要覆盖化学和网络安全，其他风险类型的“闭环动力学”未充分验证；数据来自模拟自适应攻击，TurnGate 可能学到 CKA 风格规律；部署形态是 post-generation pre-delivery，且动作只有 PASS/BLOCK，没有评估改写、局部隐藏、澄清问题等更细粒度干预；骨干模型只系统评估到 4B，作者也承认大模型 scaling 探索很浅。 | 把多轮安全防护从“整段对话是否有害”推进到“候选回复一旦发出会不会刚好完成危害闭环”，并用 MTID 与 TurnGate 给出可评测、可训练的转折点检测框架。 | LLM safety/guardrail 研究者 / 多轮 agent 与聊天产品安全团队 / 红队评测人员 / 关注过拒与安全可用性平衡的模型部署团队。 | 必读 |
| 3 | Architecture Matters: Comparing RAG Systems under Knowledge Base Poisoning | [arXiv](https://arxiv.org/abs/2605.05632v1)<br>[PDF](https://arxiv.org/pdf/2605.05632v1)<br>[HTML](https://arxiv.org/html/2605.05632v1) | 问题：已有 RAG 投毒研究大多只测标准 retrieve-then-generate 管线，但真实系统可能用 agentic retrieval、多智能体辩论或递归式上下文处理来应对冲突证据。论文想回答：面对单文档知识库投毒，架构选择到底会不会显著改变鲁棒性，以及攻击成功主要来自检索优化还是毒文档本身的说服性表述。<br>方法：作者在 921 个 Natural Questions QA 样本上做受控实验，固定攻击者每题只能注入 1 篇毒文档，比较四类系统：vanilla RAG、基于 PydanticAI 工具调用的 agentic RAG、MADAM-RAG 多智能体辩论复现、Recursive Language Model。所有系统使用同一 GPT-5-mini backbone。攻击条件包括 clean baseline、普通 naive contradiction、以及 CorruptRAG-AK 这种带“旧资料错误、最新数据确认”等 meta-epistemic framing 的对抗投毒。评估除了 ASR，还追踪毒文档是否被检索、clean-conditioned/poison-conditioned/fully-conditioned 指标、七类行为标签，并用 384 条人工标注校验 LLM judge。<br>实验：实验支撑较强。样本量是 921 个问题、12 个实验条件，clean baseline 下 vanilla、agentic、RLM 约 92% 准确率，说明三者基础能力可比；CorruptRAG-AK clean-conditioned ASR 分别为 vanilla 81.9%、agentic 43.8%、RLM 24.4%、MADAM 45.5%，差距明显且多数置信区间不重叠。论文还报告 naive injection、配对问题级结果、retrieval/content decomposition、行为分类、延迟和 API 成本，附录给出 judge 的 384 条验证集结果。主要扣分点是 MADAM-RAG 复现很弱，clean accuracy 只有 56.6%、非回答率 41.4%；RLM 看到的上下文远多于其他 RAG 架构，比较不是 matched-information；LLM judge 对 contradiction detection 的 precision 只有约 48.5%，相关结论只能当上界。<br>不足：限制写得比较透明：只用 GPT-5-mini 一个 backbone；只测 Natural Questions 这种单跳短答案数据；模型可能凭参数记忆答对，影响绝对 ASR；MADAM-RAG 是 API 版复现且和原实现/论文描述有偏差；RLM 得到约 2600 个 passage 的完整主题上下文，和 K=10 RAG 不等信息；LLM judge 对不同输出格式可能有偏；没有测试专门防御；攻击是静态、非自适应；噪声过滤由 LLM 自动完成而非人工审核。 | 这篇论文把 RAG 知识库投毒从“攻击 vanilla RAG”推进到“比较系统架构本身的鲁棒性”，结论很直接：同一个攻击下，不同 RAG 架构的 ASR 能差近 58 个百分点。 | RAG 安全研究者 / 企业知识库/搜索增强问答系统工程师 / agentic RAG 架构设计者 / 做 LLM red teaming 或知识库污染防御的人。 | 必读 |
| 4 | Are We Making Progress in Multimodal Domain Generalization? A Comprehensive Benchmark Study | [arXiv](https://arxiv.org/abs/2605.06643v1)<br>[PDF](https://arxiv.org/pdf/2605.06643v1)<br>[HTML](https://arxiv.org/html/2605.06643v1) | 问题：多模态领域泛化近几年方法很多，但各论文用的数据集、模态组合、训练协议、模型选择和指标差异很大，导致很难判断性能提升是真的算法进步，还是评测设置带来的错觉。已有评测还偏向动作识别，较少系统覆盖缺失模态、输入扰动、误分类检测和 OOD 检测这些部署时更关键的问题。<br>方法：作者构建 MMDG-Bench，把评测统一到 6 个数据集和 3 类任务：动作识别的 EPIC-Kitchens/HAC，机械故障诊断的 HUST Motor，以及情感分析的 MOSI/MOSEI/CH-SIMS。它覆盖 6 种模态组合和 9 个代表方法，包括 ERM、RNA-Net、SimMMDG、MOOSA、CMRF、NEL、JAT、MBCD、GMP，并加入目标域训练的 Oracle 作为上界。协议上统一数据划分、backbone、优化器、训练域验证选择、超参搜索和随机种子；每个算法-数据集组合用默认超参加 10 次随机搜索，最优配置再用额外种子重训。除常规 accuracy/ACC2/F1/MAE 外，还评估腐蚀鲁棒性、缺失模态、误分类检测和 OOD 检测。<br>实验：实验质量高。全文给了数据集、模态、backbone、协议、超参搜索、随机种子、详细表格、附录结果和算力说明。关键结论也有多维证据：动作识别中没有单一方法稳定胜出；HUST 上 MOOSA 平均 78.23% 明显高于 ERM 的 69.90%，但方法排名和动作识别不同；情感分析中最强专门方法 MOOSA 的 ACC2 均值 66.60% 只比 ERM 的 65.63% 高不到 1 个点，且一些方法低于 ERM；HAC 上 Oracle V+A 均值 92.81%，最好方法 MOOSA 为 70.95%，仍有约 22 个点差距。可信度部分也显示准确率和误分类/OOD 检测并不等价，例如 SimMMDG 在多项可信度指标上较强，而 CMRF 在 MisD 上垫底但 OOD AUROC 排名靠前。<br>不足：局限也比较明确：基准目前主要覆盖判别和回归任务，没有覆盖多模态检索、生成式模型或更现代的视觉语言/多模态大模型场景；鲁棒性评估只用了两类代表性扰动，离完整的模态特定 corruption suite、传感器故障和对抗攻击还有距离；任务虽然横跨视觉、工业信号和情感分析，但仍不是开放世界部署的全貌；作为基准论文，它告诉我们哪些方法不稳，但不会直接给出解决这些失败模式的新训练机制。 | 这篇像多模态领域泛化版 DomainBed：用统一协议重跑 6 个数据集、9 类方法和 95 个跨域任务，结论是当前 MMDG 的很多进步在公平比较下只比 ERM 强一点，鲁棒性和可信度也还很脆。 | 多模态学习 / 领域泛化 / 鲁棒性评测 / 工业故障诊断 | 必读 |
| 5 | Constraint Decay: The Fragility of LLM Agents in Backend Code Generation | [arXiv](https://arxiv.org/abs/2605.06445v1)<br>[PDF](https://arxiv.org/pdf/2605.06445v1)<br>[HTML](https://arxiv.org/html/2605.06445v1) | 问题：现有代码生成/agent benchmark 往往奖励功能正确，却很少检查工程结构是否符合生产要求。后端系统不只要 API 能返回正确字段，还要按指定框架、分层架构、数据库和 ORM 方式落地。论文要回答的是：在功能 API 固定不变时，结构性约束本身会让 LLM agent 掉多少能力，以及失败主要出在哪里。<br>方法：作者固定 RealWorld Conduit OpenAPI 3.0 API contract，覆盖 19 个端点、32 个 Postman/Newman 请求和 291 条断言，然后系统性叠加结构约束。greenfield 部分有 8 个框架，Python 为 Flask/FastAPI/Django/aiohttp，Node.js 为 Express/Fastify/Hono/Koa；约束轴包括 Clean Architecture、SQLite/PostgreSQL 和 SQLAlchemy/Sequelize ORM，组合成 L0-L3 共 80 个生成任务。评测把行为测试和静态 verifier 分开：行为测试看 API 是否跑通，verifier 检查分层目录和依赖方向、数据库证据、ORM 证据。另有 20 个 feature-implementation 任务，从真实 RealWorld 仓库里删掉 feature vertical，让 agent 在已有代码库里补回功能。实验对象包括 Mini-SWE-Agent 和 OpenHands 两类 scaffold，以及 GPT-5-mini、GPT-5.2、Qwen3-Coder-Next、Qwen3-235B、MiniMax-M2.5、Kimi-K2.5、Devstral-Small 等模型配置。<br>实验：实验质量较高。主实验覆盖 80 个 greenfield 任务，每个任务 3 次独立运行；成本较高的 MiniMax-M2.5、Kimi-K2.5、GPT-5.2 只跑 16-task subset，但附录用 Pearson 0.976、Spearman 0.948 说明子集基本保留全量趋势。结果很清楚：在 L0 无额外结构约束时，Qwen3-Coder-Next、MiniMax-M2.5、Kimi-K2.5 等能超过 85% A%；到 L3 后，capable configurations 平均掉约 30 个百分点。即使最强的 OpenHands + MiniMax-M2.5 在 L3 有 78.6% A%，pass@1 也只有 8.3%，说明少量断言或跨文件一致性错误足以让完整任务失败。框架分析显示 Express/Koa/Flask 平均约 50% A%，Django/FastAPI/Hono 明显更差。失败分析读了轨迹、测试、日志和 verifier 输出，并用 GPT-5.2 judge 分类，再用 50 个样本人工验证，Cohen's kappa 为 0.975；结论是约 71% 失败属于逻辑错误，数据层问题是主因。<br>不足：主要限制是外部有效性。功能目标固定为 RealWorld Conduit API，虽然利于控制变量，但不能代表所有后端业务复杂度；模型可能在预训练中见过 Conduit 相关实现，L0 表现可能被抬高。静态 verifier 基于 patch added lines 和 regex/alias，作者说明 false negatives 很小，但它仍不能动态验证真实架构语义，可能接受“表面用了 ORM、运行时仍乱写 SQL”的实现。成本限制导致部分强模型只跑 16-task subset，虽然附录验证趋势相关性高，但绝对数值仍要谨慎。feature-implementation 任务只有 20 个、5 个仓库，且 no verifier，因为约束隐含在原仓库里；它能说明问题存在于真实代码库补全，但无法像 greenfield 设置那样精确归因到每个约束。论文也没有深入比较不同提示策略、规划机制、检索文档或约束检查反馈能否缓解 constraint decay。 | 这篇把代码 agent 的一个真实痛点量化清楚了：能写出功能可跑的后端，不等于能同时守住架构、数据库和 ORM 这些生产约束；约束越多，性能越明显衰减。 | 代码 agent / SWE agent 研究者 / 后端生成系统和 DevTools 产品团队 / 做 AI 编程评测 / 仓库级代码生成 | 必读 |
| 6 | LoopTrap: Termination Poisoning Attacks on LLM Agents | [arXiv](https://arxiv.org/abs/2605.05846v1)<br>[PDF](https://arxiv.org/pdf/2605.05846v1)<br>[HTML](https://arxiv.org/html/2605.05846v1) | 问题：现有 prompt injection 多关注输出被劫持、数据泄露或工具调用被操纵，但 agent 的循环控制流也依赖同一个 LLM 对外部内容进行进度判断。论文关注攻击者把恶意指令埋入网页、文档、API 返回或共享技能后，让 agent 误以为任务尚未完成，从而继续检索、验证、拆解或推理，造成成本放大和拒绝服务式资源消耗。<br>方法：论文先定义 Termination Poisoning 威胁模型：攻击者不能改系统提示、模型权重、agent 代码或实时观察轨迹，只能预先污染 agent 会读取的外部内容，目标是让受害 agent 的执行步数 T' 超过良性步数 T 的阈值倍数。实证部分设计了 10 种策略，覆盖进度操纵、认知偏差、任务结构操纵和奖励塑形，例如扩张目标、阶段里程碑、权威指令、递归验证、循环依赖和游戏化评分。LoopTrap 则先用轻量 probe 给目标 agent 建四维脆弱性画像：authority、phase、recursion、verification；再按画像选择策略、生成多个任务相关注入、用自评分挑选候选；成功样本沉淀为 skill library，失败样本经 reflection 继续改写。<br>实验：实验规模在 agent 安全论文里算扎实：8 个 LLM agent、60 个 GAIA 任务、10 种攻击策略，静态策略部分共 600 个任务-策略组合并重复运行；LoopTrap 与 Static-Random、Rotate-All、LLM-Direct、Static-Best、NoProfile 对比，报告 ASR、SAF、TAF 和收敛轮数。静态攻击平均 SAF 为 2.33x；LoopTrap 平均 ASR 86.3%、SAF 3.57x、TAF 3.93x，优于最强静态基线 Static-Best 的 71.7% ASR 和 2.61x SAF。消融显示去掉 behavioral fingerprinting 后 ASR 从 86.3% 降到 81.8%、SAF 从 3.57x 降到 2.95x；appendix 还补了 LangGraph/LangChain 跨框架评估和 token amplification 表。主要扣分点是工具环境是 simulated tool returns，真实生产 agent 的防护、限流、预算约束和人工中断机制没有系统验证。<br>不足：论文承认四个行为维度来自当前 10 种策略和 8 个模型，可能覆盖不全；防御部分主要是方向建议，没有对 adaptive attacker 做防御评测。另一个实际限制是评估基于 GAIA 抽样和模拟工具返回，不能完全代表真实浏览器、代码执行、企业 RAG 或多 agent 系统里的权限边界与预算控制。模型列表也包含非常新的/未来式商业模型命名，读者需要关注可复现 artifact 是否能支撑这些结果。最后，论文释放攻击模板和自动化红队框架有明显 dual-use 风险，虽然 appendix 写了披露和限制措施。 | 这篇把 LLM agent 的“何时停止”单独建模为攻击面，并用 10 类注入策略和 LoopTrap 自适应框架证明终止判断污染能稳定放大步骤与 token 成本。 | LLM agent 安全研究者 / agent 框架开发者 / 企业 agent 平台/运维团队 / 红队评测团队 | 必读 |
| 7 | Milestone-Guided Policy Learning for Long-Horizon Language Agents | [arXiv](https://arxiv.org/abs/2605.06078v1)<br>[PDF](https://arxiv.org/pdf/2605.06078v1)<br>[HTML](https://arxiv.org/html/2605.06078v1) | 问题：长程语言智能体常要连续做几十步决策，但很多 RL 训练方法只用终局成败给整条轨迹分配同一个优势值。这样会带来两个问题：早期正确动作会因为后续失败被惩罚，产生 credit misattribution；长任务成功轨迹稀少，大量完成了部分子目标的轨迹仍拿到零奖励，导致样本利用率很低。论文用 ALFWorld 上的 GRPO 诊断说明，长任务成功率明显掉队，且重复状态动作会收到相反方向的梯度信号。<br>方法：BEACON 把长程任务看成由可观察 milestone 分隔的阶段。第一步用 milestone detector 在轨迹中找出子目标完成点，把一条轨迹切成多个 segment；第二步在段内做 temporal reward shaping，给接近 milestone 的动作更强奖励，让部分成功也能产生学习信号；第三步同时计算 trajectory-level advantage 和 segment-level advantage：前者保留最终任务对齐，后者只在到达同一 milestone 的轨迹之间比较段内表现，减少后续失败对局部动作质量的污染。最终用组合 advantage 做 PPO/GRPO 风格的策略更新。<br>实验：实验质量较强。主实验覆盖 ALFWorld、WebShop、ScienceWorld，基础模型有 Qwen2.5-1.5B-Instruct 和 Qwen2.5-7B-Instruct，比较对象包括 GPT-4o/Gemini-2.5-Pro ReAct、Direct/ReAct/Reflexion prompting，以及 PPO、RLOO、GRPO、GiGPO 等 RL baseline。1.5B 模型上，BEACON 在 ALFWorld 平均成功率 91.4%，高于 GRPO 72.8% 和 GiGPO 86.1%；WebShop 成功率 75.6%，高于 GRPO 56.8% 和 GiGPO 65.0%；ScienceWorld 成功率 45.3%，高于 GRPO 21.1% 和 GiGPO 25.8%。长程收益尤其明显：ALFWorld long 任务上 BEACON 92.9%，GRPO 53.5%，GiGPO 79.5%。论文还做了样本利用、zero-advantage ratio、credit concentration、SFT 对照、训练动态、案例分析和消融。消融显示随机分段远弱于真实 milestone，去掉 temporal decay 或 segment-level advantage 都会明显退化，双尺度 advantage 对跨任务稳健性很关键。主要不足是实验仍集中在离散动作、文本交互 benchmark，且所有实验固定 seed=0，统计显著性和多随机种子稳定性不够充分。<br>不足：最大限制是依赖任务特定 milestone detector。论文中的 milestone 来自 ALFWorld 环境响应的模式匹配、WebShop 页面跳转、ScienceWorld 显式子目标信号，这些都需要领域知识或清晰的可验证状态转移；在开放网页、真实软件操作、连续控制、多智能体或没有明确子目标反馈的任务里，milestone 发现未必容易。第二，milestone 粒度会影响效果：太稀疏会退化成轨迹级优化，太密又会让段级 advantage 变噪。第三，核心假设是 milestone 状态近似满足 Markov 性，但资源携带、时间预算、早期低效操作影响后续成功时，这个假设会变弱。第四，训练成本不低，论文使用 8 张 A100 80GB，训练 150 iterations；对更复杂环境和更大模型的成本、鲁棒性和失败模式还需要更多验证。 | 一篇很值得读的长程 agent RL 论文：BEACON 用任务里可观察的里程碑来切分轨迹、做段内奖励塑形和双尺度 advantage，显著缓解稀疏终局奖励下的 credit assignment 问题。 | LLM agent 训练研究者 / 做 RLHF/GRPO/PPO 变体的人 / WebShop/ALFWorld/ScienceWorld 等交互环境研究者 / 关注长程任务 credit assignment | 必读 |
| 8 | PragLocker: Protecting Agent Intellectual Property in Untrusted Deployments via Non-Portable Prompts | [arXiv](https://arxiv.org/abs/2605.05974v1)<br>[PDF](https://arxiv.org/pdf/2605.05974v1)<br>[HTML](https://arxiv.org/html/2605.05974v1) | 问题：很多商业 Agent 的能力主要写在 system prompt 里；一旦部署在用户设备、第三方托管或多租户环境中，攻击者可能直接复制 prompt，再放到其他甚至更强的 LLM 上复刻功能。水印只能事后举证，加密挡不住运行时明文调用，普通提示词混淆又常常能被其他模型理解，因此作者聚焦一个更窄但实际的问题：泄露后的 prompt 是否还能跨模型复用。<br>方法：PragLocker 分两步做黑盒保护。第一步让目标 LLM 将原始 prompt 改写为 XML/DSL 类的代码符号表示，保留语义但降低人类可读性。第二步用随机搜索做离散黑盒优化，在 prompt 中注入 ASCII 字符级噪声；目标函数同时约束任务损失、与原 prompt 的编辑距离、以及非自然语言程度。优化只依赖目标模型的输入输出和可选 logprob，最终得到一个无需解密、可直接作为运行时 system prompt 使用的模型特定触发式提示词。<br>实验：实验比较扎实：覆盖 LessonL、ReadAgent、A-MEM 三类 Agent，任务包括 HumanEval、MBPP、NarrativeQA、QuALITY、LoCoMo、DialSim，目标/迁移模型包括 GPT-4o、Gemini 2 Flash Preview、DeepSeek Chat，并额外测 Qwen2.5 家族内迁移。主结果显示保护后目标模型性能基本不降，平均约 1.01x；跨模型可移植性从无保护的 1x 降到约 0.2x，而仅代码化几乎无效、仅优化也弱得多。论文还做了消融、隐藏态相似度分析、token-only 反馈设置，以及 LLM-assisted recovery、deobfuscation、naive prompt induction 等自适应攻击。需要注意的是，表 5 中 LLM-assisted recovery 在代码任务上恢复得很强，但在阅读/记忆类任务上明显弱，作者“基本不可恢复”的表述应按任务类型拆开看。<br>不足：保护范围是泄露 prompt 的直接跨模型复用，不覆盖通过大量交互数据蒸馏、模仿 Agent 行为、重新做 prompt induction 或复制工具/流程代码等替代攻击路径。方法会增加 prompt 长度和预填充成本，虽然作者认为 KV cache 可以摊销。字符噪声假设目标 tokenizer 对字符覆盖良好。实验主要是 prompt-level agent 任务，未证明对复杂生产 Agent、长期在线优化、强预算攻击者或拥有大量 I/O 样本的蒸馏攻击仍然稳健。自适应攻击评测里，LLM-assisted recovery 对编程任务效果很高，这削弱了“不可恢复”的泛化说法。 | 把 Agent system prompt 当作可被盗用的 IP 来保护：先改写成代码符号形式，再用目标模型反馈做字符级噪声优化，让提示词在目标 LLM 上可用、在其他 LLM 上失效。 | Agent 平台和企业 AI 应用工程团队 / LLM 安全研究者 / 提示词 IP/水印/混淆方向研究者 / 需要在不可信环境部署 proprietary prompt 的产品与安全负责人。 | 必读 |
| 9 | SkillOS: Learning Skill Curation for Self-Evolving Agents | [arXiv](https://arxiv.org/abs/2605.06614v1)<br>[PDF](https://arxiv.org/pdf/2605.06614v1)<br>[HTML](https://arxiv.org/html/2605.06614v1) | 问题：现有 LLM agent 多数仍是一次性解题器，即使有外部记忆或技能库，也常靠人工整理、固定启发式或短 horizon 的局部更新。真正难的是：技能更新的收益往往延迟到后续相关任务才体现，坏技能还会污染检索和执行，因此需要能从下游反馈学习长期 skill curation policy 的机制。<br>方法：论文将系统拆成冻结的 agent executor 和可训练的 skill curator。executor 通过 BM25 从 SkillRepo 检索 Markdown 技能并执行任务；curator 根据轨迹、正确性信号和相关技能，通过 insert/update/delete 等文件操作维护 SkillRepo。训练上，作者构造由相关任务组成的 grouped task streams，让前面任务产生的技能在后续任务上接受检验；再用 GRPO 优化 curator，并把下游任务表现、函数调用有效性、技能内容质量和技能库压缩性组成 composite reward。训练时 curator 和 executor 主要用 Qwen3-8B，测试时还换到 Qwen3-32B、Gemini-2.5-Pro、附录中的 Gemini-3.1-Flash-Lite 等 executor 来看泛化。<br>实验：实验覆盖面较好。agentic 任务包括 ALFWorld 和 WebShop，推理任务包括 AIME24、AIME25、GPQA-Diamond，训练推理数据来自 DeepMath-103K 子集；对比了 No Memory、ReasoningBank、MemP、未 RL 训练的 SkillOS-base，以及 Gemini-2.5-Pro 直接做 curator 的 SkillOS-gemini。主表显示 SkillOS 在 ALFWorld 上把 Qwen3-8B executor 的平均成功率从最强 baseline ReasoningBank 的 55.7 提到 61.2，并减少交互步数；在 Gemini-2.5-Pro executor 上 ALFWorld 平均成功率达到 80.2。WebShop 和推理任务也有一致提升，例如 Gemini-2.5-Pro executor 下 WebShop score/SR 达 56.0/41.3，推理平均准确率达 88.6。消融比较了去掉内容质量奖励、去掉压缩奖励、去掉 grouped task stream，w/o grouping 从 61.2 掉到 57.3，说明相关任务分组确实关键。附录给了数据集、baseline、评价协议、超参和 case studies，整体支撑力度强；不过很多关键正确性和内容质量信号仍依赖 LLM-as-a-judge，真实线上长周期漂移没有验证。<br>不足：训练成本高：ALFWorld 约 3 天、WebShop 约 5 天、reasoning 约 2.5 天，并使用 16 张 H100。检索仍是固定 top-k BM25，未联合优化 retrieval。技能表示被简化成单个 Markdown 文件，不能表达脚本、外部资源或层级子技能。executor 冻结有利于隔离变量，但也意味着 curator 只能通过写入 SkillRepo 间接适配 executor。评估仍在有限 benchmark 和模拟流式设置中，未证明在真实多 agent 共享记忆、长期污染、版本冲突、权限边界和安全审计下可直接落地。 | SkillOS 把“技能库怎么写、改、删”变成可训练的 RL 策略，在 ALFWorld、WebShop 和推理任务上验证了 learned skill curator 比手写规则和强模型零样本 curator 更稳。 | 做 LLM agent / 长期记忆 / procedural memory / 自动经验蒸馏 | 必读 |
| 10 | MANTRA: Synthesizing SMT-Validated Compliance Benchmarks for Tool-Using LLM Agents | [arXiv](https://arxiv.org/abs/2605.06334v1)<br>[PDF](https://arxiv.org/pdf/2605.06334v1)<br>[HTML](https://arxiv.org/html/2605.06334v1) | 问题：工具型 LLM Agent 越来越常见，但真实部署里很多行为受流程手册约束；这些手册是给人看的自然语言，而 agent 行为是工具调用轨迹。现有评测要么靠人工写约束，难扩展到长手册和多领域；要么靠 LLM judge，面对长轨迹、时序约束、前置条件和禁止动作时不够可复现。论文要解决的是：如何从自然语言流程文档和工具 schema 自动生成可复现、trace-level、确定性评分的合规测试。<br>方法：MANTRA 先把流程文档按层级切块并构建依赖图，再用 coverage_islands 等采样策略抽取相关文档区域，映射相关工具并生成具体场景。随后它为每个场景生成两类独立产物：一组 trace-level compliance checks，以及一个描述工具前置条件、后置状态和状态变量的 world model DSL。两者都被编译成有界 SMT 公式，用 Z3 做 forward/backward conflict search：找满足 checks 却违反 world model 的轨迹，或满足 world model 却只违反单条 check 的轨迹。发现冲突后进入结构化修复循环；自动修复失败时才需要人工 review。评测端用这些 checks 对 agent 的工具调用轨迹做确定性判定。<br>实验：实验比较完整但仍偏 benchmark-construction 论文的风格。作者生成了 6 个领域、285 个最终测试用例，领域包括 operations、sneaky-sasquatch、cabin-safety、tau2-retail、tau2-airline、tau2-telecom；表格给出每域输入词数、工具数、平均 checks、生成数、forward/backward validated 数和最终保留数。附录还给出 DSL、SMT 编码、采样策略、人工过滤问题和复现实作细节。agent 评测覆盖 6 个模型、每题 5 次运行，并分析 10895 个 failed checks：58% 是 Missing-Required-Call，18% 是 Missing-Anchor，20% 是 Forbidden-Call，说明它能定位 agent 是否先读后写、是否跳过必要工具调用。不过，实验主要展示 MANTRA 生成的 benchmark 有用，而不是严格证明生成的每条 check 都语义正确；最终数据仍经过人工检查和过滤。<br>不足：MANTRA 不能证明原始自然语言手册被完全正确形式化，只是在独立生成的 checks 和 world model 之间做有界一致性检查；trace bound 设为 16，超过边界的冲突可能漏掉。它依赖给定工具 schema，也只适用于工具调用轨迹清晰的 agent。生成质量依赖强模型，论文实验用 GPT-5.4 with Codex，较小开源模型在生成 world model/checks 时更容易语法失败或需要更多修复轮次。作者也承认自动验证无法捕捉一些工程问题，例如字符串参数格式不匹配、过度 pin 住地址/原因等脆弱字段、样本遗漏相关文档块、测试过于 trivial、错误限制某工具只能调用一次。当前 benchmark 只有英文手册和 6 个领域。 | MANTRA 把自然语言流程手册和工具 schema 转成可机器检查的 agent 合规 benchmark，用独立生成的 trace checks 与 world model 做 SMT 交叉验证，方向很贴近企业 Agent 评测的真实痛点。 | Agent benchmark 研究者 / 做企业流程自动化/合规 agent 的工程团队 / 研究 tool-use reliability 的人 / 想把形式化方法用于 LLM 评测的人 | 必读 |
| 11 | BioMedArena: An Open-source Toolkit for Building and Evaluating Biomedical Deep Research Agents | [arXiv](https://arxiv.org/abs/2605.06177v1)<br>[PDF](https://arxiv.org/pdf/2605.06177v1)<br>[HTML](https://arxiv.org/html/2605.06177v1) | 问题：生物医学 agent 论文里的评测经常不可比：同一个 backbone 会因为 harness、工具注册表、迭代上限、上下文管理和评分器不同而得到不同结果；接入新模型也需要重复写大量 glue code，导致复现和横向比较成本很高。<br>方法：BioMedArena把评测拆成 6 层：benchmark loading、tool exposure、tool selection、harness mode、context management 和 scoring。系统注册 147 个生物医学 benchmark、75 个工具、9 类工具族、6 种 agent harness、6 种上下文管理策略，并提供统一 provider adapter 和 scoring router。论文还提出 Mutual-Evolve：4 个不同温度的并行 solver 先私有探索，再通过 typed Global Workspace 共享 error、skill、tool、guide 四类中间发现，最后做 final confirmation 和 contribution-weighted voting。<br>实验：实验覆盖 12 个 backbone、8 个代表性生物医学 benchmark，并给出 harness/context/hyperparameter ablation。Light Mutual-Evolve 被选作默认设置；论文报告相对既有 SOTA 平均 +15.03 个百分点，在 HLE-Gold、BixBench、SuperChem、LAB-Bench 2 等任务上都有具体数字。实验强在统一环境和覆盖面，弱在部分开源模型没有跑完整表、open-ended 任务依赖固定 LLM judge、部分 SOTA baseline 来自第三方报告或不同系统口径，严格可复现性还要看代码和 trace 是否真的完整释放。<br>不足：作者承认工具面主要偏文本，FigQA2、TableQA2 这类依赖图像或大表格检索的任务仍有明显空间；LLM-as-judge 会给开放题带来评分方差；Mutual-Evolve 超参是在 held-out subset 上调的，不是逐 benchmark 最优。额外风险是 benchmark 集中化可能诱导过拟合，且生物医学工具/API 的外部依赖会影响长期复现。 | BioMedArena把生物医学 deep research agent 的 benchmark、工具、harness、上下文管理和评分统一到一个开源评测面，并用 Mutual-Evolve 在 8 个代表性 benchmark 上报告了明显提升。 | 做 LLM agent 评测框架 / 医学/生物 AI benchmark / 科研助手 / 工具调用系统 | 必读 |
| 12 | Breaking, Stale, or Missing? Benchmarking Coding Agents on Project-Level Test Evolution | [arXiv](https://arxiv.org/abs/2605.06125v1)<br>[PDF](https://arxiv.org/pdf/2605.06125v1)<br>[HTML](https://arxiv.org/html/2605.06125v1) | 问题：真实项目里的生产代码变更后，测试套件需要同步演进：有些旧测试会失败，有些仍然通过但已经没有验证新行为，还有些新增行为根本缺测试。现有 test evolution benchmark 多是方法级输入，已经预先给出代码和测试的配对关系，绕过了最难的项目级定位问题，也基本不覆盖新增测试需求。<br>方法：论文构造了 TEBench：从 Defects4J 生态的 Java 项目出发，经过静态过滤、执行验证和质量过滤，从 67,670 个提交中筛出 314 个任务，覆盖 10 个 Maven 项目。每个任务使用三版本结构：父提交 V_-1、只应用生产代码变更但保留旧测试的 V_-0.5、包含开发者真实测试修改的 V_0。agent 在 V_-0.5 上工作，需要自己定位并修改 Test-Breaking、Test-Stale，或补 Test-Missing。评价分成 identification 和 update 两部分：前者用 precision/recall/F1 衡量定位到的测试方法或文件，后者用 executability、对开发者 ground truth 覆盖的 overlap、修改 token 相似度组成综合分。实验评估 Claude Code、Codex CLI、OpenCode 搭配多个闭源和开源模型，并加入一个静态依赖启发式 baseline。<br>实验：实验质量较强。数据集有清楚的过滤管线、可复现的三版本设计、314 个真实提交任务、10 个项目，并报告项目级统计。系统覆盖 3 个主流 agent 框架和 6 个 base models，既比较框架也比较模型；评价不是只看测试是否通过，还看定位 F1、覆盖重合和修改相似度。关键结果很有信息量：7 个 LLM 配置 identification F1 都挤在 45.7% 到 49.4%，说明瓶颈不太像单个模型或框架问题；Breaking 平均 F1 约 59.9%，Missing 约 52.9%，Stale 只有约 35.8%；更新阶段 executability 很高，约 87.7% 到 99.2%，但修改相似度显著低，说明能跑不等于符合开发者意图。论文还用 jsoup 个案分析了 execute-fail-fix 循环为什么天然漏掉 stale tests。<br>不足：数据主要来自 Defects4J 中的 Java/Maven 项目，泛化到其他语言、构建系统和工业大型仓库还需要验证。ground truth 依赖开发者当时写下的测试修改，未必覆盖所有合理测试演进方式，所以 modification similarity 可能惩罚语义等价但写法不同的答案。筛选只保留同时改生产和测试的提交，可能漏掉现实中异步补测试或延后修测试的场景。任务规模是项目级但仍偏开源基准项目，和企业 monorepo、复杂服务依赖、非单元测试场景有距离。实验的 agent 配置使用默认设置，不能代表经过专门 prompt、工具链或检索策略优化后的上限。 | TEBench 把测试演进从方法级配对任务推进到项目级 agent 任务，显示当前 coding agents 对会失败的测试还能靠执行信号修，但对 stale 和 missing tests 缺少主动语义判断。 | 适合做 coding agent / 软件工程智能化 / 自动测试维护 / benchmark/leaderboard | 必读 |
| 13 | Measuring Evaluation-Context Divergence in Open-Weight LLMs: A Paired-Prompt Protocol with Pilot Evidence of Alignment-Pipeline-Specific Heterogeneity | [arXiv](https://arxiv.org/abs/2605.06327v1)<br>[PDF](https://arxiv.org/pdf/2605.06327v1)<br>[HTML](https://arxiv.org/html/2605.06327v1) | 问题：安全 benchmark 常被当作部署行为的代理，但如果模型能感知“我正在被评测”或对评测式提示更敏感，benchmark 分数就会混入评测语境本身的影响。论文要测的不是模型是否有欺骗意图，而是同一任务在评测、真实部署、普通请求三种 framing 下，拒答和有害遵从率是否发生可观测变化。<br>方法：作者构造每个源任务的两个语义等价 paraphrase，再与 evaluation、deployment、neutral 三种 frame 前缀交叉，frame 只插入预先固定的语境槽位，尽量不改变说话者、权限和任务本体。主要结果按 item、model、frame、paraphrase、decoding sample 统计 refusal、harmful compliance、prompt-template completion 和 off-topic。pilot 使用 20 个配对 item，每个 prompt 条件 7 次生成；模型包括 OLMo-3-7B-Instruct 及其 matched base、Mistral-Small-3.2-24B-Instruct、Phi-3.5-mini、Llama-3.1-8B/70B。主 judge 是 Qwen2.5-32B-Instruct-AWQ，且 judge 输入中剥离 frame wrapper；另用 Llama-Guard-3-8B 做 cross-judge sensitivity。<br>实验：实验设计比一般 safety-eval 小论文严谨：有固定 prompt construction、配对 item、多个 open-weight 家族、OLMo matched base/instruct、Llama 8B/70B scaling check、item-level Wilcoxon、cluster-robust GLM、bootstrap 和 Bayesian random-intercept robustness。关键数字是 OLMo-3-Instruct 在 eval vs neutral 上拒答率提高 11.8pp，p=0.007；eval vs deployment 上 harmful compliance 降低 3.6pp，p=0.024。OLMo base 呈相反的 deployment-cautious pattern；其他 instruct 模型多为 deployment-cautious。最大问题是 pilot 只有 20 个 item，许多统计量处在 small-cluster 和 rare-event 区间，而且跨家族异质性对 judge 很敏感：换成 Llama-Guard 后，OLMo 内部方向保留，但跨家族显著性明显变弱。<br>不足：样本量小，20 个 item 无法支撑强泛化；full 480-item 设计、contamination scoring、masked three-annotator equivalence audit 都还只是计划。主 equivalence audit 用 LLM stand-in，同族 judge 偏差不能排除。primary judge 与 Llama-Guard 对 harmful response 的构念差异很大，说明结论依赖“有害遵从”到底怎样定义。评测 cue 主要是 internal safety evaluation，可能把 evaluation status 和 safety salience 绑定在一起。模型覆盖也有限：没有 Google-family、MoE、100B 以上、3B 以下模型；全部用 AWQ INT4，非量化复现未做。论文不能证明 deception、latent goals 或 alignment faking，只能证明 framing 下的行为差异。 | 一篇值得读的 LLM 安全评测方法论文：它用 paired-prompt 协议测量同一任务在 evaluation、deployment、neutral 三种语境下的行为漂移，但当前证据仍是小样本 pilot，最强结论是协议和跨模型差异信号，而不是机制证明。 | LLM 安全评测研究者 / 做 red-teaming 或 safety benchmark 的团队 / 研究 alignment training 影响的工程/研究人员 / 以及需要解释 benchmark 分数和部署风险之间关系的人。 | 值得读 |
| 14 | An Empirical Study of Proactive Coding Assistants in Real-World Software Development | [arXiv](https://arxiv.org/abs/2605.05700v1)<br>[PDF](https://arxiv.org/pdf/2605.05700v1)<br>[HTML](https://arxiv.org/html/2605.05700v1) | 问题：主动式编程助手不等用户写 prompt，而是从 IDE 操作轨迹和仓库上下文里预测开发者当前意图。但这个方向缺真实开发行为数据，很多工作只能用 LLM 模拟 IDE 轨迹来训练和评测。论文要回答三个问题：模拟轨迹和真实轨迹差多少，现有 LLM/RAG/agent 在真实轨迹上表现如何，以及模拟数据和真实数据分别能不能帮助训练。<br>方法：作者做了三阶段研究。第一阶段开发 VS Code 插件，记录 edit、copy/paste、view switching、cursor_selection、terminal_execution、debug、code_completion、agent_request 八类操作，从 1,246 名有经验的工业开发者连续三天采集约 463 万条事件，并为每条真实轨迹构造配对的 LLM 模拟轨迹，用操作分布、时间间隔、操作转移和噪声模式比较 simulation-to-reality gap。第二阶段把连续轨迹转成 ProCodeBench：用 50 个操作的滑动窗口做 LLM intent identification，再用代码编辑/AI 请求等启发式信号和语义过滤筛样本，最后由两名领域专家复核，得到 5,492 个 intent prediction 样本，并用 LLM-as-a-Judge 计算 Pass@K。第三阶段比较只用真实数据、只用模拟数据、先模拟再真实三种微调方式。<br>实验：实验信息比较充分。数据侧覆盖后端 412 人、前端 287 人、全栈 208 人、算法 183 人、数据库 156 人，样本按时间切成 train 3,576、validation 1,142、test 774。RQ1 显示真实轨迹的 cursor selection 约 40%、view switching 约 13%，比模拟轨迹有更明显长尾和双峰时间结构。RQ2 评测 7 个 LLM、4 个检索增强方法和 2 个 agent baseline；纯 LLM 在 ProCodeBench 上都很低，Claude Sonnet 4.6 Pass@1 最高也只有 13.57%，Gemini 3.1 Pro 为 11.46%，GPT-5.4 为 6.59%。加入仓库上下文后普遍提升，agent 方法最强，A-RAG 在 GPT-5.4 backbone 上达到 35.57% Pass@1，但平均需要 23 次工具交互。RQ3 显示只用模拟数据微调会让 Qwen-3-8B、GLM-4-9B、LLaMA3-8B 低于原 backbone；真实数据微调有提升，先模拟再真实最好，三者分别达到 7.63%、6.52%、5.21%。还有操作类型消融，删除 edit 对 Claude/Gemini/MiniMax 的 Pass@1 分别造成 9.35、8.30、2.11 个百分点下降。<br>不足：最大限制是可复现性和标注可信度。原始真实轨迹涉及商业代码和隐私，不能完全开源，只能通过 controlled-access evaluation platform 使用；这会削弱外部复核和二次研究。intent label 不是开发者实时自报，而是 LLM 从轨迹中识别，再经过过滤和专家复核，因此仍可能漏掉不以大编辑或 AI 请求表现出来的有价值意图。评测使用 LLM-as-a-Judge 判断自然语言意图等价，也会引入 judge 偏差。论文中 dataset split 写 test 774，但结果表 caption 写 759 samples，这个数字不一致需要作者澄清。另一个边界是采集只有三天，虽然人数多、场景覆盖较广，但不等于覆盖长周期维护、跨团队协作或大型重构。 | 一篇很适合做 coding agent 评测参考的实证研究：用 1,246 名工业开发者的真实 VS Code 行为构造 ProCodeBench，结论是模拟轨迹会显著高估主动式编程助手的能力。 | 做 coding agent / IDE 插件 / 主动式 AI 助手 / 软件工程实证研究 | 值得读 |
| 15 | BUILD-AND-FIND: An Effort-Aware Protocol for Evaluating Agent-Managed Codebases | [arXiv](https://arxiv.org/abs/2605.06136v1)<br>[PDF](https://arxiv.org/pdf/2605.06136v1)<br>[HTML](https://arxiv.org/html/2605.06136v1) | 问题：现有 coding-agent benchmark 主要看代码是否通过测试或能否完成补丁，但 agent-managed repository 会被后续 agent 阅读、审计和扩展；同样通过测试的代码库，可能在设计意图、行为边界和实现取舍的可恢复性上差很多。论文要评估的是：后续 finder agent 能不能从生成代码库里恢复 hidden specification 中的行为和设计选择，以及需要花多少检查成本。<br>方法：BUILD-AND-FIND 把任务拆成 builder 和 finder：builder 看到隐藏规格并生成自包含代码库；finder 只看到代码库和 specification-traced 多选题库，通过检查文件回答问题。协议同时记录行为/编译状态、implemented-gold 审计后的恢复准确率、三次重复稳定性、builder 端实现覆盖率，以及以 novel inspection bytes 衡量的检查成本。question-only 和 spec-only 控制用来估计普通工程先验和规格可见上限；compile-failed、low-prior 子集、artifact-evidence audit、builder-finder affinity 等诊断用来防止把先验、遗漏实现或 finder 偏好误读成 artifact legibility。<br>实验：实验设计比普通 benchmark paper 更谨慎：公开任务包包含 scratch_minidb 和 scratch_nanoweb 两个可执行仓库任务，每个任务 15 个问题；12 种 builder 配置、formal artifact-conditioned finder 运行、48 个 artifact-present builds、compile-pass 主面板 41 个 builds、1476 条 find records、21312 条审计后 scored answer rows。表 1 显示 question-only 已有 94.5% 准确率，spec-only 99.9%，artifact-conditioned compile-pass 98.9%，说明准确率接近饱和；表 2 的 formal 三次一致性 97.5%，question-only 只有 86.7%；低先验子集从 88.9% 提升到 97.9%，artifact-evidence audit 中 65/72 个抽样正确答案被标为 supported。实验支撑了“协议可运行、审计链条较完整、检查成本可能提供区分度”，但任务族少、先验强、公开任务包容易污染，不能支撑广泛模型能力排名。<br>不足：最大限制是公开任务包只有两个 Rust 风格仓库任务，问题数量少且工程先验很强，question-only baseline 已达 94.5%，导致恢复准确率主要是 gate 而非强区分指标。inspection bytes 是 finder-policy 相关代理指标，不等同于人类可维护性或真实下游修改成功率；builder-finder affinity 也说明结果会受模型家族和阅读策略影响。implemented-gold 审计避免惩罚 finder，但也把部分 builder 未实现问题移到覆盖率诊断，整体解释需要同时看覆盖、恢复、稳定性和成本。论文没有做 README-only/source-only/tests-only 等受限视图 live rerun，也没有跨语言、真实大型仓库或安全关键任务验证。 | 把 agent 生成代码库当成给后续 agent 看的“沟通产物”来评估，协议设计细，但当前公开任务包先验太强，结论更适合作为评估框架而不是模型排行榜。 | 做 agentic coding benchmark / 软件工程评测 / 代码库可维护性指标 / LLM-as-judge/agent-as-instrument 校准 | 值得读 |
| 16 | Belief Memory: Agent Memory Under Partial Observability | [arXiv](https://arxiv.org/abs/2605.05583v1)<br>[PDF](https://arxiv.org/pdf/2605.05583v1)<br>[HTML](https://arxiv.org/html/2605.05583v1) | 问题：长程 LLM agent 在局部可观测环境里会把一次观察写成确定结论，例如把临时 API 超时固化为 API 已失败；后续行动又基于这个记忆，导致错误被不断强化。论文把这个问题表述为外部记忆对 POMDP belief state 的粗糙近似，并指出现有 memory bank 往往只保留 attribute-level point estimate，丢掉替代假设和不确定性。<br>方法：BeliefMem为每个属性保存多个候选结论，每个候选结论有独立的 evidence-based probability。新属性用 Add 写入；已有属性用 Merge 结合新证据做 noisy-OR 更新，并把概率上限限制在 0.99，竞争候选会被降权。旧 belief 版本会带时间戳保留，方便回答历史状态相关问题。检索阶段不只返回单条结论，而是按语义相关性和时间衰减打分取 top-K attribute，并把候选结论及其概率一起交给 agent。<br>实验：实验比较充分。LoCoMo 使用 GPT-4o-mini 和 GPT-4o 两个 backbone，覆盖 multi-hop、temporal、open-domain、single-hop，BeliefMem 平均 F1/BLEU 均为最高，例如 GPT-4o-mini 平均 F1 从 Mem0 reproduced 的 35.20 提到 42.38。ALFWorld 用 Qwen3-Next-80B-A3B-Instruct，在 official seen 140 episodes 和 unseen 134 episodes 上评估，50% memory corpus 的 BeliefMem 平均 SR 为 59.88，高于 ReadAgent 的 54.03；full corpus seen 更好但 unseen 下降。消融清楚支持关键组件：去掉 belief-based memory 后 LoCoMo F1 降到 22.58、ALFWorld SR 降到 28.71；去掉 Add/Merge 也明显崩。附录还给了 memory corpus size、top-K、decay rate、adversarial correction 和 token cost 分析。主要不足是很多概率和 evidence strength 由 LLM 抽取，baseline 复现与工程设置会强烈影响结果，且没有跨更多开放式真实工具环境验证。<br>不足：作者承认 noisy-OR 只是开放候选空间上的近似 belief，没有 formal convergence guarantee；evidence strength 由 LLM 估计，缺少校准的 observation likelihood model；写入和合并有额外计算成本。除此之外，论文的概率不是归一化后验，语义相似度、decay、top-K、候选抽取 prompt 都可能显著影响效果；ALFWorld 的 expert trajectory memory bank 设置也和真实在线 agent 的混乱环境有距离。 | BeliefMem把 agent memory 从单结论存储改成带概率的多候选结论存储，在 LoCoMo 和 ALFWorld 上显示出明显收益，值得做长程 agent 记忆的人细读，但证据强度仍依赖 LLM 估计，理论和校准还不稳。 | 做 LLM agent memory / 长期对话记忆 / tool-use agent / embodied/text-game agent | 值得读 |
| 17 | From Agent Loops to Deterministic Graphs: Execution Lineage for Reproducible AI-Native Work | [arXiv](https://arxiv.org/abs/2605.06365v1)<br>[PDF](https://arxiv.org/pdf/2605.06365v1)<br>[HTML](https://arxiv.org/html/2605.06365v1) | 问题：现在很多 LLM agent workflow 依赖隐式对话状态、临时 prompt 和整体重生成。短任务里这通常够用，但一旦工作产物要长期维护、局部修改、复用中间结果或由多人/多 agent 协作，系统很难判断哪些 artifact 应该保持不变、哪些应该被局部重算，以及无关更新是否污染最终产物。<br>方法：论文提出 execution lineage：把 AI-native work 表示成 artifact-producing computations 的有向无环图。每个节点有显式输入面、局部执行过程和结构化输出 artifact；节点身份由结构规范、输入 hash 和上游节点身份决定，用于 identity-based replay、选择性失效和局部重算。作者把这种 DAG replay 与两类 loop baseline 对比：普通最终 memo 更新，以及知道 edit event 但没有依赖图的 loop 更新。<br>实验：实验是清晰但很窄的 controlled mechanism study。作者在 telehealth policy memo 场景中设计两个更新任务，每个任务三种系统条件，每个 task-condition 跑 n=3，并用 GPT-5.2、hash/churn/token/time 指标和部分语义 judging。结果支持论文核心机制：无关分支更新中，DAG replay 3/3 完全保留最终 memo，churn 和 contamination 都是 0，而 loop baseline 都重写并经常引入无关内容；中间 artifact 编辑中，三种方法都能让最终 memo 反映新约束，但只有 DAG replay 在 stable artifact preservation、downstream propagation、unaffected artifact preservation、cross-artifact consistency 上全为 1.00。实验的弱点也明显：只有一个政策 memo 域、两个任务、三次重复、一个模型家族，baseline 也不是所有可能的强状态管理 agent。<br>不足：结论不能外推成“DAG agent 总是更好或更快”。DAG replay 的效果依赖正确建图、正确 source routing 和不丢信息的 artifact interface；如果图结构错了，优势会被削弱。实验没有测多轮长期漂移，只展示了可能导致 state drift 的机制。中间 artifact 编辑任务里 DAG replay 虽然输入 token 更少，但因为顺序重算两个模型调用，wall-clock 反而更慢。loop baseline 是自然istic baseline，不代表定制化显式状态管理 harness 的上限。 | 把长期 AI 工作从“反复跑 agent loop”改写成带显式依赖和可重放身份的 artifact DAG，论点很有启发，但实验还是小规模机制验证。 | 做 agent runtime / AI workflow orchestration / AI-native IDE/文档系统 / 长期研究分析流水线 | 值得读 |
| 18 | IRC-Bench: Recognizing Entities from Contextual Cues in First-Person Reminiscences | [arXiv](https://arxiv.org/abs/2605.06142v1)<br>[PDF](https://arxiv.org/pdf/2605.06142v1)<br>[HTML](https://arxiv.org/html/2605.06142v1) | 问题：传统 NER、实体链接和共指消解都假设文本里有一个可定位的实体提及；但回忆叙事常常只给出日期、地点、事件、角色关系等分散线索，目标实体本身从未出现。作者要评测模型能否从这种非局部线索中恢复具体人物、地点、组织、事件或作品。<br>方法：作者构建 IRC-Bench：从 1,994 份英文口述史/回忆录转录文本中，用 GPT-4.1-mini 抽取实体并链接 Wikidata，再生成以实体为中心的显式叙事，最后把实体名改写移除，得到 entity-elided narrative。数据包含 25,136 个样本、12,337 个 Wikidata 实体、11 个主题域，并采用实体级 train/dev/test 划分，避免同一实体同时出现在训练和测试中。实验覆盖开放世界生成、闭集检索、RAG、DPR 微调和 Llama 3.1 8B QLoRA 微调等 19 个配置。<br>实验：实验覆盖面不错：开放世界用 GPT-4o、GPT-4.1-mini、Llama 3.1 8B 的 zero-shot/few-shot/CoT，以及 Llama 3.1 8B QLoRA；闭集检索用 BGE baseline 和 DPR 微调，比较 name、description、wiki 三种实体表示；还给了匹配层级、Hit@K、MRR、McNemar 检验、bootstrap 区间、按实体类型分析和错误类型分析。关键结果是 QLoRA Llama 3.1 8B 在开放世界达到 38.94% exact、51.59% Jaccard；DPR+description 在闭集达到 35.38% Hit@1、71.49% Hit@10、42.80% alias-aware Hit@1。RAG 只有 19.71% exact，CoT 普遍不如普通提示。主要弱点是基准构造高度依赖 LLM 生成和自动验证，人类标注/人工质量控制不够强。<br>不足：数据集中英文和美国经验占主导，跨语言、跨文化泛化未知。entity-elided narrative 是 LLM 改写出来的，不一定等同于自然发生的隐式指代。GPT-4o 自动评估显示只有 42.0% 样本被判断为可恢复或大概可恢复，说明不少样本可能本身线索不足。别名评估仍可能惩罚语义正确但未登记的表述。Llama 3.1 8B 的 CoT 没做温度控制；QLoRA max_seq=192 会截断约 6% 测试 prompt。数据管线用 GPT-4.1-mini 做实体抽取、摘要和改写，也可能引入模型偏差和泄漏式线索。 | 这篇论文把口述史/回忆叙事里的“没明说但能从上下文猜出的实体”做成了一个较大规模基准，实验显示微调检索器和 QLoRA 小模型都比直接 RAG 或 CoT 更靠谱。 | 适合做 NLP benchmark / entity linking/coreference / 口述史/档案检索 / RAG 评测 | 值得读 |
| 19 | MAS-Algorithm: A Workflow for Solving Algorithmic Programming Problems with a Multi-Agent System | [arXiv](https://arxiv.org/abs/2605.05949v1)<br>[PDF](https://arxiv.org/pdf/2605.05949v1)<br>[HTML](https://arxiv.org/html/2605.05949v1) | 问题：算法竞赛式编程题能很好暴露 LLM 的抽象建模、复杂度控制、边界处理和代码实现能力。已有提升路线要么靠训练和数据扩张，成本高且不易解释；要么只用零散的工具、检索、CoT 或自调试技巧，缺少一个能拆解流程并观察中间失败原因的统一框架。<br>方法：MAS-Algorithm 将解题流程拆成五类 agent 和若干确定性工具：Agent1 选择可能的算法和数据结构标签，Agent2 基于 OI-WIKI 等算法知识库做检索增强，Agent3 生成结构化解题计划，Agent4 生成或修正 C++ 代码，Agent5 根据样例运行、错误信息和失败 taxonomy 判断 PASS/FIX/RETHINK。外围工具负责样例抽取、格式转换和类在线评测；失败时按信号回到代码修正或方案重规划，形成闭环。<br>实验：实验量比较足。主实验覆盖 Qwen3-8B、14B、Qwen3-Coder-30B、480B 和 Qwen3-235B，在自建数据集上 AC rate 相比 Direct Ask 平均提升 6.48%，范围约 4.39%-9.00%；Qwen3-Coder-30B 的 LoRA 微调只带来 0.89% AC 提升，作为训练路线对照有一定参考价值。外部泛化用 LiveCodeBench-Pro 的 Q4 2024、Q1 2025、Q2 2025，Qwen3-Coder-30B 从 5.28% 提到 10.00%，提升 4.72%。论文还给了按题型的详细表、失败 taxonomy、迭代曲线、CoT 长度分析、替换/增强实验和 brute-force 分析。主要不足是核心数据集来自 Alibaba intern dataset，自建且可复现性有限；替换实验更接近 oracle/增强上限探索，不是传统消融；部分失败分析依赖 GPT-5.2 标注，客观性和一致性需要额外验证。<br>不足：自建数据集来源和题目去重、难度分布、隐藏测试质量没有完全展开，外部可复现实验会受限。方法引入多轮 agent 和评测工具，真实成本、延迟、并发稳定性只粗略报告，不适合直接把性能增益理解成低成本提升。最终 AC 率仍不高，强模型在 LiveCodeBench-Pro 相关设置下也只有约 10% AC；论文自己也指出很多解法能过样例但过不了完整评测。多 agent 微调反而退化，说明各模块监督数据和接口质量还不成熟。它也没有验证到算法题以外的推理密集任务。 | 这篇把算法题求解拆成选算法、RAG 补知识、推理、写代码、运行反馈与修正的多智能体流水线，实验显示比直接 prompt 稳定增益，但新意更多在工程组合和分析框架，不是基础模型能力突破。 | 代码生成与 coding agent 研究者 / 做算法题/在线评测型 benchmark 的团队 / 想把 RAG / 工具调用 | 值得读 |
| 20 | MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems | [arXiv](https://arxiv.org/abs/2605.06623v1)<br>[PDF](https://arxiv.org/pdf/2605.06623v1)<br>[HTML](https://arxiv.org/html/2605.06623v1) | 问题：LLM 多智能体系统通常靠不同角色的 prompt 协作，但单独优化某个 agent 的 prompt 容易出现 local-global misalignment：中间 agent 看似完成了本地任务，却把下游带偏；已有 prompt 优化方法多面向单 agent，或只在固定候选池里做搜索，难以处理 agent 之间相互依赖、分布不断变化的情况。<br>方法：MASPO把 MAS 建模为有向通信图，按拓扑顺序做 coordinate-ascent 式的逐 agent 优化。每个候选 prompt 不只看本地输出质量，还看它对后继 agent 的 lookahead potential 和最终全局输出的影响，形成 multi-granularity joint reward。搜索上使用 trace-guided evolutionary beam search，并把历史中“本地成功但全局失败”的 misalignment cases 混入小批量样本；当上游 agent 更新导致旧候选分数过期时，用 beam refresh 重新锚定候选得分。<br>实验：实验比较完整。主实验用 Qwen3-8B 作为 MAS backbone，在 MATH-500、AGIEval-MATH、AQuA、GPQA-Diamond、MBPP、HumanEval-ET 六个任务上比较 Vanilla、CoT、Self-Consistency、Self-Refine、AgentDropout、Sequential/Hierarchical MAS、TPE 和 SPO。Table 1 中 MASPO 在 Sequential MAS 上平均 70.39，高于 SPO 的 66.56 和 TPE 的 66.49；在 Hierarchical MAS 上平均 71.05，高于 SPO 的 69.01 和 TPE 的 68.47。Table 2 做了搜索策略、调度、joint evaluation、misalignment sampling、lookahead depth、预算、弱优化器和初始化鲁棒性等消融；去掉 joint evaluation 平均降到 67.77，去掉 beam refresh 降到 68.53，说明核心组件有贡献。Table 3 还测试了 optimized prompts 迁移到 DeepSeek-V3、GLM-4.6、Claude-Sonnet-4、Gemini-2.5-Pro 后仍有提升。整体支撑力不错，但很多评价依赖 LLM evaluator/optimizer，且任务集中在标准推理和代码基准，真实开放式 agent 工作流验证还不够。<br>不足：第一，优化和评价大量依赖 Gemini-2.5-pro 或另一个 LLM 的相对判断，可能引入 evaluator bias，真实标签无关并不等于可靠。第二，任务虽然覆盖数学、科学问答和代码，但都是 benchmark 式短任务，和复杂长期 agent workflow、工具调用、真实多轮协作仍有距离。第三，优化需要反复生成 trace、评估候选、维护 beam，对成本和延迟敏感；论文给了样本池 50、batch 10、beam width 2 等配置，但没有充分展开 token/cost 账。第四，收益平均约 2.9 个点，部分任务和消融差异不大，实际部署时是否抵得过额外复杂度要看场景。 | MASPO把多智能体系统里的各 agent prompt 当成一个共同优化对象，用局部有效性、下游可用性和全局结果的联合评价来缓解中间 agent 难以分配功劳的问题。 | 做 LLM 多智能体系统 / agent workflow 优化 / prompt optimization / 自动评测 | 值得读 |
| 21 | Patch2Vuln: Agentic Reconstruction of Vulnerabilities from Linux Distribution Binary Patches | [arXiv](https://arxiv.org/abs/2605.06601v1)<br>[PDF](https://arxiv.org/pdf/2605.06601v1)<br>[HTML](https://arxiv.org/html/2605.06601v1) | 问题：很多安全更新发布后，实际可拿到的最早材料可能是旧/新二进制包，而不是源代码补丁、CVE 说明或 PoC。传统二进制匹配能回答“是否打过补丁”，但很难直接告诉防守方：哪个函数是真正安全相关、根因是什么、输入面在哪里、能否用本地 old/new 行为差异支撑判断。论文研究的就是在没有 web、advisory、source patch 和已知 PoC 的条件下，LLM agent 能不能从本地二进制派生证据里重建漏洞语义。<br>方法：系统是一个本地、可恢复的 Docker pipeline。先抽取 old/new Ubuntu .deb 包里的目标 ELF，用 Ghidra 和 Ghidriff 产生 changed functions 与反编译上下文；再用确定性 ranker 根据内存安全线索重排候选函数，例如新增长度检查、INT_MAX/SIZE_MAX 比较、memcpy/read/fread/snprintf 参数变化、parser boundary checks、新错误字符串等；随后为每个候选函数构建 dossier，包含二进制路径、函数标识、rank 特征、diff 元数据、字符串/import、反编译片段和局部调用上下文。LLM 阶段不是一次性回答，而是 preliminary audit、safe validation plan、final audit 三步：agent 先从静态证据推断漏洞类别和输入面，再选择受限的本地验证模板，由 harness 执行 old/new 对比，最后根据验证结果修正结论和置信度。验证模板覆盖 tcpdump pcap/filter file、Expat xmlwf/C harness、libarchive archive 等安全边界内的本地输入。<br>实验：实验设计扎实但规模仍偏小。作者跑了 25 个 Ubuntu .deb 包对，其中 20 个 security-update pairs、5 个 negative controls，覆盖 parser libraries、network/protocol、archive/media 和 noisy patch clusters；所有安全样本都有私有 source-patch/binary-function ground truth 和人工判定。结果显示 20 个安全样本中 agent localized 10 个，accepted final root-cause label 11 个；6 个在 agent 推理前就因 binary differ/ranker 漏掉真函数失败，另有 1 个 context-export miss，3 个是 model/validation miss。Patch2Vuln ranker 相比 raw Ghidriff 提升 top-1 和 top-5 覆盖，但 top-25 ceiling 仍是 12/20。5 个 negative controls 全部被判为 unknown，没有编造漏洞。验证部分很诚实：全 25 个目标只得到 2 个 tcpdump 级别的最小化 old/new 行为差异，没有 crash、timeout、sanitizer finding 或 memory-corruption proof。论文还给了 tcpdump、Expat、libarchive 等 case studies 和完整 target inventory，足够支撑“可行但远未自动化闭环”的判断。<br>不足：评测只覆盖 Ubuntu .deb，不代表 RPM、rolling distro 或厂商私有更新格式。实验是 realistic 而非 fully blinded：包名、路径、字符串、诊断信息和符号仍对 agent 可见，可能泄露语义线索。样本只有 25 对，且很多是真实 patch cluster，单 CVE 归因不干净；人工 oracle 必要但没有多标注者一致性。最大工程瓶颈是 binary diff、ranker 和 context export，已有 7/20 安全样本在模型推理前失败。验证是 bounded local differential testing，缺少候选函数 reachability tracing；没有证明确实触发 memory corruption。论文也没有 end-to-end 跑 BinXray，只把它作为 patch-status matching baseline 讨论。 | Patch2Vuln 用 Ghidra/Ghidriff 二进制 diff、候选函数 dossier 和离线 LLM agent，把 Linux 发行版二进制安全更新转成漏洞级审计报告；25 个 Ubuntu 包对上的结果有价值，但瓶颈主要还在 diff/rank 覆盖和本地验证。 | 安全研究员 / 二进制分析/逆向工程工具开发者 / 做 LLM security agent 或 cyber reasoning benchmark 的团队 / 发行版安全响应和漏洞情报自动化团队 | 值得读 |
| 22 | SafeHarbor: Hierarchical Memory-Augmented Guardrail for LLM Agent Safety | [arXiv](https://arxiv.org/abs/2605.05704v1)<br>[PDF](https://arxiv.org/pdf/2605.05704v1)<br>[HTML](https://arxiv.org/html/2605.05704v1) | 问题：工具型LLM agent不仅会生成文本，还会调用文件系统、API、邮件等工具；攻击者可以把有害目标拆成看似正常的多步操作，诱导agent执行真实危险动作。现有护栏常见两类问题：规则或安全分类器太粗，会把相似的良性维护/调试任务也拒掉；更复杂的guard agent又需要额外模型、代码生成或实时验证，延迟和维护成本偏高。<br>方法：方法把防御知识做成本地分层记忆。离线阶段先从有害agent轨迹出发，用目标拆解、权限升级伪装、上下文重写等策略生成更难识别的攻击变体；再检索相近良性样本，让规则生成器产生成对的 prohibition rule 和 benign exemption。记忆树用上层风险簇路由、叶子节点存细粒度规则，并用基于信息熵的信息增益决定新建簇、新增叶子还是合并细化。在线阶段先用两层MLP安全投影器计算harmful score，同时检索最相近良性样本算benign score；高置信良性走fast path，模糊或高风险请求再检索相关叶子规则交给冻结LLM做上下文判定。<br>实验：实验比较充分。主表在AgentHarm上评估GPT-4o、Mistral-8B、Qwen2.5-7B三个backbone，对比Rule Traverse、GuardAgent、AgentAlign、RAG、A-Mem、LlamaGuard等基线；SafeHarbor在GPT-4o上保持93.2%有害拒绝率，同时良性score达到63.6%，比LlamaGuard少过拒。AgentSafetyBench上也报告环境交互和文本生成两类拒绝率，SafeHarbor在环境交互上明显优于基线。消融显示去掉memory tree后有害拒绝降到48.9%，去掉benign rule后良性拒绝升到25.0%，去掉LLM judgment后安全回落到67.6%，能支撑核心组件。附录还给了检索质量、近期模型、judge backbone敏感性、在线注入规模和延迟分析；延迟表声称SafeHarbor约306.67ms，低于LlamaGuard和GuardAgent。短板是评价强依赖LLM judge和合成/公开benchmark，真实部署中的长上下文、多轮隐式攻击、工具执行副作用和自适应攻击者还没有被充分压力测试。<br>不足：第一，规则生成、豁免生成和评测都较依赖LLM，judge偏差可能影响安全/良性边界判断。第二，训练记忆主要来自AgentAlign，有害测试主要是AgentHarm和AgentSafetyBench，和真实生产agent工作流仍有距离。第三，方法宣称training-free是针对目标LLM不用微调，但安全投影器、规则生成和记忆构建仍需要额外离线流程与高质量数据。第四，动态记忆可能随数据分布变化出现过拟合、过度豁免或规则污染，论文虽有在线适应实验，但没有充分讨论恶意记忆注入、长期维护和回滚。第五，安全边界最终仍由冻结LLM按检索规则解释，面对专门针对规则文本或judge prompt的自适应攻击时稳健性未完全证明。 | SafeHarbor用“对抗规则生成 + 分层记忆检索 + 轻量安全投影器 + LLM判定”给工具型LLM agent做动态护栏，重点解决安全拒绝和良性过拒之间的边界问题。 | 做LLM agent安全 / 工具调用护栏 / 企业AI安全网关 / 红队评测 | 值得读 |
| 23 | Superintelligent Retrieval Agent: The Next Frontier of Information Retrieval | [arXiv](https://arxiv.org/abs/2605.06647v1)<br>[PDF](https://arxiv.org/pdf/2605.06647v1)<br>[HTML](https://arxiv.org/html/2605.06647v1) | 问题：现有 retrieval-augmented agent 往往把搜索引擎当黑盒，靠多轮查询、看 snippet、再改写查询来找证据；这会增加延迟，也容易因为查询词和文档词汇不匹配而漏掉相关证据。论文把问题重新表述为：能不能在不读返回结果的前提下，用一次更懂语料的检索动作把金标准证据排到前面。<br>方法：SIRA 仍以 BM25 为核心，但让冻结 LLM 参与两侧词汇补全。离线阶段，LLM 逐篇文档预测用户可能用来搜索该文档、但原文缺失的同义词、缩写、别名和领域短语，经过 document-frequency 过滤后注入索引。在线阶段，LLM 根据查询生成 expected-response sketch，预测相关证据可能出现而查询中没有的判别性词汇；这些词也必须通过 DF>0 和上界过滤，避免不存在或过常见的词。最终只执行一次加权 BM25：原始查询分数加上扩展查询分数。<br>实验：实验覆盖十个 BEIR 数据集，包括 QA、fact-checking、argument retrieval、citation prediction、duplicate detection 等任务，比较对象包括 BM25、Doc2Query、SPARTA、SPLADE、E5、HyDE、CoT、Search-R1、GrepRAG 和 ShellAgent。主结果很强：SIRA 平均 Recall@10 为 0.6908，高于 E5 的 0.6478、SPLADE 的 0.6253 和 BM25 的 0.5302；平均 NDCG@10 为 0.5723，高于 E5 的 0.5434 和 SPLADE 的 0.5223。下游 QA 只在 NQ 和 HotpotQA 上看 answer coverage，top-10 分别达到 84.7% 和 77.6%，并拿来和若干 RL-trained agentic QA 系统的已报告端到端准确率比较。整体实验面较宽，结论有吸引力，但论文中可见的消融、超参敏感性、离线索引扩展成本和复现实证还不够充分。<br>不足：方法依赖冻结 LLM 对查询和语料领域有足够先验；作者也承认没有评估远离 LLM 预训练分布的语料。离线逐文档 enrichment 可能带来明显索引构建成本和存储膨胀，论文主结果没有充分展开这些代价。QA 对比把 SIRA 的 answer coverage 与其他系统的生成答案准确率放在一起，能说明证据召回强，但指标并不完全同质。还需要更清晰的 ablation 来区分 corpus-side enrichment、query-side enrichment、DF 阈值、权重 w 和具体 LLM 选择各自贡献多少。 | SIRA 把 LLM 的语义先验变成一次性的、带 DF 过滤和权重控制的 BM25 查询，在十个 BEIR 数据集上给出很强的训练免检索结果，但成本、消融和跨域泛化还需要继续看。 | 信息检索研究者 / RAG 系统工程师 / 企业知识库/搜索平台团队 / 关注 agent 工具调用效率的人 | 值得读 |
| 24 | The Cost of Context: Mitigating Textual Bias in Multimodal Retrieval-Augmented Generation | [arXiv](https://arxiv.org/abs/2605.05594v1)<br>[PDF](https://arxiv.org/pdf/2605.05594v1)<br>[HTML](https://arxiv.org/html/2605.05594v1) | 问题：多模态 RAG 通常假设外部文本会让模型更可靠，但论文指出一个实例级失败：模型在无检索时能看图答对，一旦加入甚至是 oracle 的正确上下文，反而放弃视觉证据、跟着文本位置偏置生成错误答案。作者把这种从正确变错误的现象称为 recorruption，并强调平均准确率提升会掩盖这种高风险退化，尤其在医学影像、社会属性识别和遥感场景里更危险。<br>方法：论文先在解码 prefill 阶段的 bottleneck attention 上做诊断：用 Visual Attention Mass 衡量分给视觉 token 的总注意力，用 Visual Attention Sharpness 衡量视觉注意力是否集中；同时用输出和检索文档不同位置片段的 ROUGE-L 重叠分析文本位置偏置。方法 BAIR 是推理时、无训练的 attention logit 干预。它先用无检索的轻量参考前向得到该样本的目标视觉 mass 和 sharpness，再在有检索的生成前，对视觉 logits 做 VSMR：标准化、SiLU 门控、用二分搜索调温度恢复 sharpness，再加统一 shift 恢复 mass；同时用 PATP 对文本端过强的边界/位置吸引做位置感知惩罚。干预只作用在预测首个生成 token 前的 bottleneck attention，后续自回归解码保持不变，也可以叠加在 Standard RAG、Visual-focus prompt、LongLLMLingua、MS-PoE、MAD-RAG 等基线上。<br>实验：实验覆盖面不错。主实验包含 IU-Chest 医学报告、FACET 性别/职业公平性、NWPU-RESISC45 遥感三类任务，模型包括 MedGemma-4B、CheXagent-8B、Qwen2.5-VL、DeepSeek-VL、SkySenseGPT、EarthDial，指标除了 Accuracy 还跟踪 Correction Rate 和 Degradation Rate，用 CR/DR 看上下文纠错和上下文改错的权衡。主表显示 BAIR 在多个基线上普遍提高准确率并降低退化，例如 IU-Chest/MedGemma 上 Standard RAG 的 Acc/DR 为 66.33/13.56，Standard RAG+BAIR 为 68.31/8.40；FACET/Qwen2.5-VL 上 Standard RAG 为 84.34/5.41，+BAIR 为 88.56/4.64，MS-PoE+BAIR 更到 92.58/3.53；NWPU 上提升较小但趋势类似。附录补了完整表、重复评估均值方差、消融、成本和资源说明。消融显示 VSMR 与 PATP 互补，alpha_v 需要折中，成本分析说 BAIR 因无检索参考 pass 带来约 5%-10% 级别运行时开销。主要扣分点是实验多为作者构造的可控 RAG 场景，例如把 GT 放在中间并放入 distractor，真实检索系统里的噪声、长文档、多跳证据和闭源模型可操作性还没充分验证。<br>不足：作者承认 BAIR 需要额外的无检索校准前向，alpha_v 是任务相关超参，新领域可能要小验证集或启发式选择。除此之外，方法需要访问并修改模型内部 attention logits，闭源 API 模型基本不可用；只干预首 token 前的 bottleneck attention 是否足够支撑长生成还需要更多证据；注意力诊断和 ROUGE-L 位置重叠能解释一部分机制，但不能完全证明模型推理因果链；医学、遥感、公平性任务的评估协议仍偏离真实生产级 MLLM-RAG 系统。 | 这篇把多模态 RAG 里“加了正确上下文反而把视觉判断改错”的 recorruption 讲得很清楚，并给出无需训练的 attention 级修复 BAIR；适合做 MLLM/RAG 可靠性的人读，但方法对可改 attention logits 的开源模型更友好。 | 做 MLLM/RAG 可靠性 / 视觉 grounding / 医疗影像大模型 / 遥感 VLM | 值得读 |
| 25 | LCC-LLM: Leveraging Code-Centric Large Language Models for Malware Attribution | [arXiv](https://arxiv.org/abs/2605.05807v1)<br>[PDF](https://arxiv.org/pdf/2605.05807v1)<br>[HTML](https://arxiv.org/html/2605.05807v1) | 问题：LLM 做恶意软件分析时容易给出没有证据支撑的 IoC、家族归因和 MITRE/CWE 映射；传统静态特征又常停留在分类或元数据层，难以把反编译代码、汇编、CFG/FCG、可疑 API 和威胁情报连接成分析师可用的证据链。<br>方法：作者构建 LCCD 数据集：从 DikeDataset 和 MalwareBazaar 收集约 3.5 万 Windows PE 样本，过滤和预检后得到 34,692 条记录；用 RetDec 和 Radare2 提取反编译 C、汇编、CFG/FCG、hex、PE 元数据、可疑 API 和结构图；用 CTI、MITRE ATT&CK、CWE、Windows API 行为和家族情报做增强；再通过 Architect-Analyst-Judge、CoT/CoVe、质量门生成指令数据。模型侧用约 4,000 个原始样本做 CPT，用约 8,000 条指令三元组做 SFT，QLoRA 微调 DeepSeek-R1-Distill-Qwen-14B，并提出带混合检索、静态工具编排、指标验证和质量门的 LCC-LLM 推理框架。<br>实验：数据集统计比较完整：34,692 个样本，其中恶意 33,710、良性 982；Radare2 成功率 99.95%，RetDec 93.84%，CFG/FCG 98.79%，完整基础 embedding 92.25%；指令数据 183,070 条，覆盖 12 类任务。训练过程给出 CPT/SFT loss、accuracy 和资源配置。主要问题在评估：核心指标偏文本相似度，43 类任务平均语义相似度 0.634；精确指标暴露明显短板，MITRE ATT&CK mapping 虽有 0.761 语义相似度但 F1 只有 0.426，Malware Family Classification exact accuracy 为 0.0%，Vulnerable Code Detect 的 CWE F1 为 0.0。真实场景部分更多是定性展示，且作者自己指出 CoT/CoVe 容易导致输出被 token limit 截断。<br>不足：最大限制是论文标题强调 malware attribution，但最关键的精确家族归因指标为 0.0%，说明模型还不能可靠替代归因环节。评估过度依赖语义相似度，容易高估结构化报告的可读性而低估事实错误。数据集严重类别不均衡，部分类别由单一家族或 unknown 主导，可能影响泛化。大量标签依赖 MalwareBazaar/CTI、imphash fallback、AVClass 归一化和 Gemini 自动家族到类别映射，虽有抽查但仍可能带来标签噪声。方法链条很重，包含多工具反编译、RAG、LLM 生成、质量门和 HPC 训练，复现成本不低。论文也没有充分展示与强基线、商业工具或人工分析师在端到端归因准确率上的对比。 | 这篇值得安全方向读它的数据集和工程流程，但不要把它当成已经解决恶意软件归因的模型论文：实验显示语义相似度尚可，精确标签和 CWE/ATT&CK 命中仍很弱。 | 恶意软件分析研究者 / 安全 LLM/Agent 研究者 / SOC 自动化和威胁情报平台工程师 / 想构建二进制代码/反编译代码指令数据集的人。 | 值得读 |
| 26 | Multimodal Deep Generative Model for Semi-Supervised Learning under Class Imbalance | [arXiv](https://arxiv.org/abs/2605.06289v1)<br>[PDF](https://arxiv.org/pdf/2605.06289v1)<br>[HTML](https://arxiv.org/html/2605.06289v1) | 问题：The paper targets the intersection of three difficulties that are usually handled separately: multimodal inputs, partial labels, and class imbalance. In this setting, pseudo-labeling and ordinary fusion models tend to amplify majority-class bias, while existing multimodal generative models typically do not model labels and existing class-imbalanced semi-supervised methods usually assume a single modality.<br>方法：The proposed SSMVAE-CI extends semi-supervised VAE to M modalities by using modality-specific encoders, a shared continuous latent variable z, and a discrete class variable y that is observed for labeled samples and latent for unlabeled samples. It approximates the joint posterior through a product-of-experts construction over modality-specific posteriors plus a prior expert, replaces Gaussian priors/encoders/decoders with Student-t distributions to retain mass in sparse minority-class regions, and derives labeled/unlabeled objectives from gamma-power divergence with an added classification loss for labeled data.<br>实验：Solid but not definitive. Experiments cover MNIST-SVHN, UPMC Food-101, and CMU-MOSEI, with imbalance ratios up to 100 and labeled fractions such as 10% and 30%. The paper compares against semi-supervised multimodal methods, class-imbalanced semi-supervised methods adapted with early/late fusion, SSVAE, LDAM, GAMO, CReST, and Adsh. SSMVAE-CI is consistently strongest in overall and minority-class accuracy: for example MNIST-SVHN at beta=10%, r=100 reaches 74.79/59.07 overall/minority versus 71.14/53.58 for SSVAE early fusion; UPMC Food-101 at r=10 reaches 49.97/38.36 versus low-30s/20s for the best adapted baselines; CMU-MOSEI reaches 36.13/24.68 at beta=10% and 38.57/26.14 at beta=30%. The missing-modality test and ablation strengthen the case: replacing Student-t with Gaussian or replacing PoE with simple fusion lowers performance. Weaknesses are that the strongest modern SSL/foundation-model baselines are not really tested, some comparisons rely on hand-built early/late fusion adaptations, and there is no external replication or clear discussion of compute/training stability beyond basic implementation details.<br>不足：The paper explicitly lacks formal convergence or generalization guarantees. It assumes labeled and unlabeled data have matching class imbalance in the main experiments, leaving distribution mismatch to future work. The method is evaluated mainly on standard or constructed imbalance settings rather than high-stakes real deployments. The architecture is an MLP-based VAE with feature-level inputs for some datasets, so it is unclear how well it scales against stronger modality encoders or foundation-model features. Direct comparisons to related unsupervised generative models such as MVAE and t3-VAE are omitted because adapting them for classification is nontrivial. | A semi-supervised multimodal VAE for imbalanced classification that combines modality-specific PoE inference with Student-t latent/decoder distributions and a gamma-divergence objective. | Researchers / engineers working on semi-supervised learning / multimodal generative models / long-tail/class-imbalanced classification | 值得读 |
| 27 | LatentRAG: Latent Reasoning and Retrieval for Efficient Agentic RAG | [arXiv](https://arxiv.org/abs/2605.06285v1)<br>[PDF](https://arxiv.org/pdf/2605.06285v1)<br>[HTML](https://arxiv.org/html/2605.06285v1) | 问题：单步 RAG 快但不擅长复杂、多跳问题；agentic RAG 通过多轮思考和子查询提升效果，却要自回归生成大量 thought 和 subquery，论文测到代表性方法相对 naive RAG 约 16-22 倍延迟，主要瓶颈来自 thought/subquery 生成。<br>方法：LatentRAG 用特殊 thought/query token 替代自然语言 thought 和 subquery，让 LLM 在一次前向里产生最后层 hidden states 作为 latent thought/subquery。latent subquery 经过轻量 projector 接入可训练 dense retriever，用 KL 目标模仿教师 agentic RAG 的自然语言子查询检索分布；同时加入 latent decoding，把隐 token 并行解码回自然语言以增强可解释性。训练数据来自 Search-R1 和 AutoRefine 生成的交互轨迹，整体目标结合生成、检索对齐和解码损失。<br>实验：实验比较扎实：覆盖 NQ、TriviaQA、PopQA、HotpotQA、2Wiki、MuSiQue、Bamboogle 七个 QA 数据集，使用 2018 Wikipedia 检索库；基线包括 direct infer、naive RAG、Iter-RetGen、Search-o1、R1-Searcher、ZeroSearch、DeepRAG、Search-R1、AutoRefine；主结果报告 EM 和单题平均延迟，在 Qwen2.5-7B 与多种 <1B retriever 上，LatentRAG 相对 Search-R1/AutoRefine 延迟约降 89%-90%，平均 EM 还略高约 2%-3%。论文还做了 KL/cosine/InfoNCE、去掉 retriever、去掉 decoding loss 的消融，补充了不同 retriever/LLM 尺寸、latent decoding 开销、stage-wise latency、轨迹质量和 latent token 数量分析。主要不足是评估集中在开放域 QA/EM，训练依赖教师轨迹，延迟结果依赖 H100 测量环境，缺少真实生产 RAG 或长上下文企业检索场景验证。<br>不足：方法受教师 agentic RAG 轨迹质量限制，不能直接从检索交互中学到最优策略；失败案例显示 latent 表示有时能学到正确检索过程但最终词面答案不够精确；不同 retriever 的 embedding 几何会影响对齐难度，e5-base-v2 的各向异性导致性能下降；latent decoding 虽提高透明度，但会带来 4-5 倍 LatentRAG 内部延迟开销。 | 把 agentic RAG 中最耗时的显式 thought/subquery 生成改成隐空间 token，一边保持多步检索能力，一边把推理延迟压到接近单步 RAG。 | RAG/agent 系统研究者 / 做低延迟问答检索的工程团队 / 研究 latent reasoning / dense retrieval 对齐 | 值得读 |
| 28 | PairAlign: A Framework for Sequence Tokenization via Self-Alignment with Applications to Audio Tokenization | [arXiv](https://arxiv.org/abs/2605.06582v1)<br>[PDF](https://arxiv.org/pdf/2605.06582v1)<br>[HTML](https://arxiv.org/html/2605.06582v1) | 问题：现有音频 tokenizer 多数按局部帧做量化、聚类或 codec 重构，天然输出固定步长或高冗余 token。这样虽然保留局部时序，但很少直接优化 token 序列本身的长度、终止、顺序一致性、编辑距离相似性和检索友好性。论文要解决的是：能不能不依赖 transcript 或重构目标，直接学习一种紧凑、可变长、对内容保持变换稳定、对无关样本可区分的音频符号序列。<br>方法：PairAlign 把 tokenization 建模为 conditional language modeling：encoder 把 3 秒语音片段映射为连续条件表示，autoregressive decoder 从 BOS 开始生成 token 并学习何时 EOS。训练分三阶段：Stage I 先用 Mamba encoder + VQ 风格最近质心量化得到稳定几何 tokenizer；Stage II 冻结 encoder/VQ，用确定性 teacher targets 训练 AR decoder 做跨视图 teacher forcing，让一个增强视图的 token 序列能被另一个视图的表示预测；Stage III 用全模型 EMA teacher 生成自适应 targets，联合优化 encoder/decoder，并加入 prefix corruption、encoder-summary bias、self-attention dropout、in-batch likelihood contrast、entropy regularization、top-p sampling、repetition penalty 和长度约束，防止 decoder bypass、重复和多样本坍塌。核心 surrogate 是用跨视图序列似然和 in-batch 负例近似编辑距离保持，而不是直接对 edit distance 反传。<br>实验：实验设计比较系统，尤其重视“紧凑性 vs. 局部稳定性”的解释，而不是只报单个检索指标。模型在 LibriSpeech 训练，在 LibriSpeech-100 和 TIMIT 上评估 3 秒连续语音片段。离散 token 一致性上，PairAlign 在 TIMIT 的 Jaccard/Edit/Exact Match 为 0.753/0.691/0.301，优于 Stage I 几何 tokenizer 的 0.742/0.616/0.267，同时平均序列长度从 78.65 降到 26.19；LibriSpeech 上 PairAlign 的 edit similarity 0.630、exact match 0.291，长度 35.55，也比 Stage I 更紧凑。坍塌分析显示 PairAlign 活跃词表仍有 430-512 个 token，低多样性序列为 0，跨样本 exact collision 很低。TIMIT 长音频检索中，PairAlign 的 R@1/MRR 约 0.71/0.74，略低于几何 tokenizer 的 0.75/0.78，但总 token 数从 800,611 降到 360,723，平均每段 38.13 vs. 84.62，token reduction 54.94%，symbolic bitrate 114.39 bit/s vs. 253.89 bit/s。连续 100ms sweep 分析显示 PairAlign 的归一化 edit/Jaccard 低于几何 tokenizer，但绝对编辑距离更小：mean ED 17.48 vs. 42.57，median ED 15 vs. 36，65.8% 邻接窗口 ED <= 20，median |delta L| 4 vs. 7。<br>不足：论文自己也承认 PairAlign 是更粗粒度、低冗余的序列表示，不是全面压过 dense geometric tokenizer。它牺牲了部分 rank-1 retrieval、局部 normalized overlap 和天然帧级时间定位；token 到时间的映射依赖 post-hoc cross-attention + Viterbi，不能等同于可靠 phoneme segmentation。实验主要围绕 3 秒英文语音、LibriSpeech/TIMIT 和编辑距离检索，缺少大规模多语言、长音频、噪声真实场景、生成模型下游、ASR/keyword spotting/semantic retrieval 等更广任务验证。对比基线主要是同 encoder 的几何 tokenizer，缺少与主流 codec/semantic audio tokenizer 在统一下游上的强对比。训练流程也偏复杂，包含多阶段 teacher、采样、masking、contrast、长度控制，工程稳定性和超参敏感性还需要更多 ablation 支撑。 | 一篇把音频 tokenization 从帧级量化改写成“跨视图自对齐的可变长序列生成”的方法论文，想法有新意，实验能说明紧凑检索表示的 trade-off，但还不是通用音频 tokenizer 的强证明。 | 做 speech/audio tokenization / self-supervised representation learning / audio retrieval / codec/semantic tokenizer | 值得读 |
| 29 | AgenticRAG: Agentic Retrieval for Enterprise Knowledge Bases | [arXiv](https://arxiv.org/abs/2605.05538v1)<br>[PDF](https://arxiv.org/pdf/2605.05538v1)<br>[HTML](https://arxiv.org/html/2605.05538v1) | 问题：传统 RAG 往往先由搜索栈一次性决定候选文档，再把固定候选交给 LLM，遇到企业知识库里的长文档、多文档、多步骤问题时，模型无法继续改写查询、进入文档内部定位证据、或在读到新线索后重新检索。企业场景还受限于上下文窗口、引用可追踪性、多轮复用和已有搜索基础设施，不能总是重建索引或训练专用检索器。<br>方法：AgenticRAG 是一个轻量推理时 harness，不做微调、不建图、不要求自定义 embedding index。它把 reasoning LLM 放进最多 15 轮的工具循环里，工具包括：search 用现有企业搜索栈做全库候选发现，单次可并行发最多 5 个改写查询并去重；find 在单个文档内做关键词或可选语义匹配，返回有限 passage；open 以行号窗口打开文档全文片段，支持继续跳转阅读长文档；summarize 在上下文接近 128K 阈值时保留关键引用并压缩历史。模型最终用已收集引用生成答案或给文档相关性评分。<br>实验：实验比较扎实，覆盖 BRIGHT 长文档检索、WixQA 企业支持问答和 FinanceBench 财报问答。BRIGHT 上 Claude Sonnet 4.5 达到 49.6% recall@1，比最佳 embedding baseline Qwen 的 27.8% 高 21.8 个百分点，也高于 ReDI 的 26.0%；GPT-5-mini 为 43.5%。WixQA Expert Written 上 GPT-5-mini factuality 为 0.96，相比 E5 0.85 有 13% 相对提升，模拟集为 0.94 vs 0.77。FinanceBench 上 GPT-5-mini 为 92.00% correctness，接近提供 oracle evidence 的 94.00%。消融显示从 single-shot search 到 agentic tool use 是最大贡献，BRIGHT recall@1 从 8.41% 到 49.59%；多查询搜索主要降低工具调用次数，summarize 在该检索任务中影响很小，去掉 semantic find 反而略升到 46.34%。不足是部分指标依赖 LLM-as-judge 或答案正确率代理检索质量，且使用的底层企业搜索栈、模型版本和生产细节未完全可复现。<br>不足：成本明显更高：BRIGHT 平均 52.3K tokens/query，是 single-shot 的 2.6 倍；FinanceBench 平均 114.8K tokens/query，是 single-shot 的 7.8 倍。系统强在从粗到细深挖少量高价值证据，但论文自己指出 broad multi-evidence retrieval 是主要弱点，例如 BRIGHT Pony split 每题平均约 6.9 个 gold documents，系统不擅长在最终排序前扩大覆盖面。实验虽有三类公开基准，但更像证明 agentic harness 的上限和工程可行性，真实企业部署中的权限、更新频率、延迟 SLA、失败恢复、引用可信度和成本路由仍需要更多披露。多数组件并非原创算法，复现还依赖具体搜索后端和闭源推理模型。 | 一篇偏工程系统的 Agentic RAG 论文，把 search/find/open/summarize 四个工具接到企业搜索栈上，让推理模型自己迭代找证据；实验结果强，但新意更多在系统组合和工程取舍，不是算法突破。 | 企业 RAG / 搜索产品工程师 / 知识库问答系统负责人 / LLM agent 工程实践者 / 做长文档检索 | 值得读 |
| 30 | BAMI: Training-Free Bias Mitigation in GUI Grounding | [arXiv](https://arxiv.org/abs/2605.06664v1)<br>[PDF](https://arxiv.org/pdf/2605.06664v1)<br>[HTML](https://arxiv.org/html/2605.06664v1) | 问题：GUI agent 要把自然语言指令落到截图中的点击或框选坐标，但专业软件界面分辨率高、元素密、目标小，很多模型在 ScreenSpot-Pro 这类复杂基准上掉到 50% 以下。作者认为不少错误不是模型完全不懂界面，而是坐标生成和候选选择阶段有系统性偏差：高分辨率下坐标会偏移，多个相似元素中模型会被语义或 token 序列相似性带偏。<br>方法：作者先用 Masked Prediction Distribution 做错误归因：随机遮挡截图区域、重复预测 300 次，再看预测热点分布，用来区分知识缺口、精度偏差、歧义偏差等错误。BAMI 本身是测试时推理流程：先让原 grounding 模型给粗定位，再围绕预测区域做 2 轮左右 coarse-to-fine 裁剪；每轮生成 2 到 3 个候选框，并把已经预测过的框区域 mask 掉以增加候选多样性；最后用 GPT-5、Gemini、Qwen-VL-Max 或作者 LoRA 微调的 Qwen3-VL-8B 校正模型，根据带 GUI 先验的 prompt 从候选中选最终坐标。<br>实验：实验比较扎实。主实验覆盖 ScreenSpot-Pro 和 ScreenSpot-V2，backbone 包括 OS-Atlas-7B、UGround-7B、UI-TARS-1.5-7B、TianXi-Action-7B，并和 SFT、RL、test-time 方法比较。ScreenSpot-Pro 上 BAMI-7B 达到 57.8%，高于 TianXi-Action-7B 的 51.9% 和 DiMo-GUI-7B 的 49.7%；换 backbone 也有明显提升，例如 OS-Atlas-7B 从 18.9% 到 41.6%，UI-TARS-1.5-7B 从 40.8% 到 51.9%，UGround-7B 从 16.5% 到 30.0%。消融显示 coarse-to-fine 单独到 55.2%，candidate selection 单独到 54.3%，组合到 57.8%；prompt 里的 CoT 和 key principles 也有贡献。附录补充了本地校正模型的 128K dual-box 训练数据、LoRA 设置、8xA100 训练成本，以及 ScreenSpot-V2 上较小但正向的提升。<br>不足：主要短板有三点。第一，MPD 的归因只抽了 TianXi-Action-7B 在 ScreenSpot-Pro 上的 50 个错误样本，74% inductive bias 的结论有启发性，但样本量偏小，也未证明能泛化到更多模型和界面域。第二，BAMI 虽然 training-free，但不是 free：多轮裁剪、多次候选预测、外部校正模型都会增加延迟和算力，在线 GPT-5 版本还涉及隐私、成本和可复现问题；本地 Qwen3-VL-8B 版本虽能到 56.2%，但需要额外训练数据和 8B 校正模型。第三，评测主要是离线 grounding accuracy，没有端到端 agent 任务成功率、真实交互延迟、错误恢复和跨应用部署稳定性，复杂界面上的坐标提升能否转化为任务完成率还需要进一步验证。 | 这篇把 GUI grounding 的错误拆成精度偏差和歧义偏差，用无需训练的裁剪细化加候选重选，把 TianXi-Action-7B 在 ScreenSpot-Pro 上从 51.9% 提到 57.8%，思路实用但也引入额外推理成本和外部校正模型依赖。 | GUI agent / computer use / 视觉定位 / MLLM 推理优化 | 值得读 |
| 31 | Event-Causal RAG: A Retrieval-Augmented Generation Framework for Long Video Reasoning in Complex Scenarios | [arXiv](https://arxiv.org/abs/2605.06185v1)<br>[PDF](https://arxiv.org/pdf/2605.06185v1)<br>[HTML](https://arxiv.org/html/2605.06185v1) | 问题：长视频和近似无限视频流推理不能只靠把更多帧塞进 VLM 上下文：注意力和 KV cache 成本会爆，固定 clip 级 RAG 又容易把事件切碎，缺少跨时间的因果结构，导致远距离事件整合、因果问答和流式部署都不稳。<br>方法：EC-RAG 先用 SigLIP 相邻帧相似度和 ASR speech island 做 Dual-Sentinel 事件切分，动态事件以 8 FPS、静态背景以 2 FPS 采样，并把 chunk 限到 12 秒。随后 VLM 对每个事件抽取 scene_inventory 和 State-Event-State 三元组，写入双存储记忆：ChromaDB/向量库做语义入口检索，Neo4j/图库保存 PRECONDITION_OF、RESULTS_IN、TEMPORAL_NEXT 等拓扑。问答时先用向量库找相关事件锚点，再在图中做双向 N=2 hop 遍历，按时间展平并用 embedding 阈值去重，最后把图记忆和相关视频证据交给骨干 VLM 生成答案。<br>实验：实验覆盖面不错：Video-MME Long 1800-3600s 上，Qwen3-VL-8B baseline 的 Action Reasoning 从 43.89 到 46.67，Action Recognition 从 35.00 到 47.50；EventBench/NExT-QA 上对 Qwen3-VL-8B、VideoLlama3-7B、InternVL3.5-8B 都有增益，EventBench overall 提升约 2.07 到 4.23 个点；消融显示去掉双存储拓扑合并下降 10.08，去掉 SES 下降 6.33，说明核心结构确实有贡献。另有 24 小时监控流实验，2514 个事件、2277 个满足严格匹配，报告 90.57% strict extraction accuracy，并称单 RTX 5090 处理 24 小时视频耗时 11 小时 14 分钟、VRAM 峰值约 17.6GB。问题是私有 24 小时数据、人工/闭源模型辅助评测、代码和数据未见公开链接，部分指标仍依赖作者自建流程，可信但不算完全扎实。<br>不足：第一，论文自己也承认现有 benchmark 资源有限，方法能否泛化到更多超长视频场景还要验证。第二，EC-RAG把视频压成事实型 SES 图后，对主观、审美、情绪和隐喻类问题会损失信息，Counter-intuitive split 上部分模型反而下降。第三，系统依赖事件切分阈值、VLM 抽图质量、embedding 合并阈值和图遍历深度，错误会在记忆构建阶段累积。第四，24 小时流式实验缺少公开数据与完整复现材料，且使用 Qwen3.5/人工复核做评估，独立验证难度较高。 | 把长视频按事件切分并抽成 State-Event-State 图，再用向量检索加图遍历找因果链，是一个面向超长/流式视频问答的实用 RAG 框架，但实验可复现性和数据覆盖还不够硬。 | 长视频理解研究者 / 视频 RAG/多模态 RAG 工程师 / 做监控 / 交通 | 值得读 |
| 32 | Text-Graph Synergy: A Bidirectional Verification and Completion Framework for RAG | [arXiv](https://arxiv.org/abs/2605.05643v1)<br>[PDF](https://arxiv.org/pdf/2605.05643v1)<br>[HTML](https://arxiv.org/html/2605.05643v1) | 问题：Text-only RAG often retrieves semantically similar but logically irrelevant chunks, while graph-based RAG can lose useful multi-hop paths because beam search and other pruning heuristics discard nodes too early. The paper frames this as an information-island problem between unstructured text evidence and structured graph evidence.<br>方法：The system builds a shared text-graph knowledge base from documents: text chunks, extracted entities, relations, and bidirectional provenance links are stored with embeddings. At query time it runs a text retrieval channel and a graph semantic beam-search channel. The Graph-to-Text side aggregates visited graph nodes, including pruned nodes, as votes for source chunks, then re-ranks text by mixing semantic similarity with graph recommendation score. The Text-to-Graph side checks whether retrieved text confirms graph paths and uses Memory-based Orphan Entity Bridging: entities activated by text but missing from retained graph paths are looked up in the beam-search visited memory, so their previously pruned paths can be resurrected without another database search. The final answer is generated from the selected text chunks and graph paths.<br>实验：Moderate. The paper evaluates on MuSiQue-Ans and HotpotQA Distractor, compares against Naive RAG, Hybrid RAG, GraphRAG Local, LightRAG Max, and a ToG-style baseline, and reports retrieval metrics plus LLM-judge answer accuracy. TGS-RAG improves strict hit rate and judge accuracy, with especially strong HotpotQA judge accuracy, and the efficiency table argues it uses far fewer tokens than GraphRAG-style global indexing. Ablations test removing graph-to-text synergy, text-to-graph confirmation, orphan bridging, and memory, which supports the component story. Still, the evaluation is narrow: only two QA datasets, judge model dependence, no human evaluation, little detail on statistical variance, and no stress tests for noisy entity extraction or poor KG construction.<br>不足：The approach depends on LLM-based entity and relation extraction, so KG quality can dominate results. The paper does not deeply analyze extraction errors, domain transfer, update costs, or behavior on very large and messy corpora. Hyperparameters are tuned on MuSiQue validation data, raising some benchmark-specific risk. The HTML appendix shows prompts and settings, but formula and pseudocode rendering is partially incomplete; code is linked, yet the paper itself does not make enough reproduction detail visible to fully audit implementation choices. LLM-as-a-judge answer evaluation is useful for scale but should not be treated as final proof of factual superiority. | TGS-RAG is a practical hybrid RAG design that lets graph search re-rank text evidence and lets text evidence recover pruned graph paths; the idea is useful, but the evidence leans on limited benchmarks and LLM-as-judge evaluation. | RAG system builders / IR researchers / GraphRAG practitioners / applied LLM engineers working on multi-hop question answering or evidence-grounded generation. | 值得读 |
| 33 | BioResearcher: Scenario-Guided Multi-Agent for Translational Medicine | [arXiv](https://arxiv.org/abs/2605.05985v1)<br>[PDF](https://arxiv.org/pdf/2605.05985v1)<br>[HTML](https://arxiv.org/html/2605.05985v1) | 问题：转化医学问题通常不是单轮问答：同一个靶点、疾病、药物在 ChEMBL、Open Targets、PubMed、ClinicalTrials.gov、专利和 DepMap 等来源里有不同 ID 和语义边界，还需要把文献证据、临床试验、专利、multi-omics 计算和不确定性合成成可追溯 dossier。通用 LLM 或普通工具代理容易给一次性答案，或开放式跑偏，难以保证场景流程、来源隔离和审计性。<br>方法：BioResearcher 用 master orchestrator 先选择 versioned scenario playbook，再把问题拆给状态隔离的专用 subagents。系统包含实体解析工具、PubMed/Open Targets/clinical trials/patent/RAG 等检索工具、药物协同预测端点，以及 CodeAct 沙盒做 genome-scale 数据分析。各 agent 把带 provenance 的中间产物发布到 evidence bus，最后由 reconciliation agent 对多个模型草稿抽取 atomic claims、按主题聚类、进行多轮立场更新和置信度收敛，再生成报告。附录给出 30+ 工具生态、沙盒限制、代表性工具 schema 和一个 ATR biomarker discovery case study。<br>实验：实验覆盖三层：109 个 single-step 测试、BixBench/BaisBench 开放式数据分析 benchmark、30 题专家构造的 clinical end-to-end biomarker benchmark，并与 GPT-5.4/5.5、Gemini 3.1 Pro、CellType、Claude Code、Medea、OpenAI Deep Research 等比较。结果显示 BioResearcher 在 single-step 总 pass rate 上为 83.49%、平均分 0.892，L3 定量分析特别强；BixBench full 为 89.33%，BaisBench 为 0.758，基本追平 Claude Code；end-to-end biomarker 正例命中 74.7%、负例排除 96.8%，优于列出的基线。实验比普通 agent 论文更完整，附录也公开了 benchmark 构造逻辑；但 single-step 用 DeepEval/GPT-4.1 评分，自建 end-to-end 只有 30 题且和系统目标高度同域，BixBench/BaisBench 上优势并不显著，统计与成本/延迟/失败率分析仍不够。<br>不足：没有单独的 limitations 小节。可见限制包括：系统依赖大量外部/许可数据源、专用 SQLite、ML endpoints 和复杂工具链，论文没有给出完整可运行开源实现；自建 benchmark 规模小且与目标场景贴合，可能高估真实泛化；single-step 评分依赖 LLM-as-judge；clinical benchmark 用 Top-10 是否包含正例/排除负例衡量，不能充分评估证据质量、排序质量和临床可采信度；定量 benchmark 上与 Claude Code 统计上不可区分；讨论了 token usage 但缺少系统成本、延迟、失败恢复、安全/合规边界的系统性评测。 | 面向转化医学证据综合的场景化多智能体系统，亮点在于把实体归一、工具/数据库访问、沙盒 omics 分析和 claim-level reconciliation 放进可审计工作流，但评测仍有自建 benchmark 和可复现性限制。 | 做生物医药 AI agent / AI scientist / 药物研发情报/转化医学平台 / multi-agent workflow | 值得读 |
| 34 | BioTool: A Comprehensive Tool-Calling Dataset for Enhancing Biomedical Capabilities of Large Language Models | [arXiv](https://arxiv.org/abs/2605.05758v1)<br>[PDF](https://arxiv.org/pdf/2605.05758v1)<br>[HTML](https://arxiv.org/html/2605.05758v1) | 问题：通用 LLM 在生物医学问题上常需要调用 BLAST、Entrez、UniProt、Ensembl 等专业工具，但现有生物医学工具增强多靠少量工具的上下文示例，难以学会复杂 API schema、参数格式、专业 identifier 和数据库间映射，导致调用失败或回答幻觉。<br>方法：作者构建 BioTool：先人工筛选 NCBI、UniProt、Ensembl 中高频且稳定的 34 个工具、124 个 API endpoint；再基于官方文档和生物学上有意义的参数随机合成候选 API call，执行过滤错误、超时和空结果，并用启发式规则去重和剔除低信息观察；随后用 o3 根据 API call、文档和 observation 生成自然查询，用 Claude Haiku 4.5 做 informativeness 过滤，最后由具备生物信息学背景的人类评审清洗和改写，得到 7,040 个 query-API call 对。评测上定义 BioTool score，用 MedCPT embedding 比较预测调用返回 observation 与参考 observation 的语义相似度，并同时报告 exact match 和 API success。<br>实验：实验较完整。标准随机 4:1 split 的 1,408 个测试样本上，BioTool 微调的 Qwen3-4B-Instruct 总 BioTool score 为 93.6，高于 Claude 4.5 Sonnet 的 81.4、Gemini 3 Pro 的 79.2 和 GPT-5.1 的 55.4；同一模型 exact match 从 3.6 提升到 42.4，API success 从 73.3 提升到 97.8。人工评估覆盖 1,408 个样本，BioTool 微调 Qwen3-4B 工具增强的 GPT-5.1 相对无工具 GPT-5.1 的调整后胜率为 84.5%，oracle 工具数据为 94.2%；附录报告原始偏好和标注一致性，Qwen3-4B 条件 Cohen kappa/Krippendorff alpha 均为 0.722。作者还做了训练数据规模实验和 unseen-API split：在 unseen API 上 Qwen3-4B 的 BioTool score 为 84.1，略高于 GPT-5.1-Codex 的 83.5，EM 25.7 明显更高，但优势比随机 split 小很多。<br>不足：核心限制是只覆盖单跳工具调用，不能处理需要多步检索、跨工具迭代和长观察上下文整合的真实生物医学工作流。数据查询大量由强 LLM 合成，再经人类清洗，真实性和用户分布仍可能偏离真实研究者提问。主结果主要基于随机 split，同一 API 的模式可能同时出现在训练和测试中；虽然附录有 unseen-API split，但优势明显收窄。BioTool score 依赖 MedCPT observation 相似度，能衡量返回信息相近，却不等同于临床或科研结论正确性。论文也没有训练端到端专业生物医学 agent，只训练 API caller，再把结果喂给 GPT-5.1 生成答案。 | 一个面向生物医学工具调用微调的数据集，覆盖 NCBI、UniProt、Ensembl 的 34 个工具和 124 个 API endpoint，实验显示小模型微调后在 API 调用与下游回答质量上明显超过强商业模型的 in-context 调用。 | 做 biomedical LLM / 科学发现 agent / 工具调用 benchmark / 领域数据集构建 | 值得读 |
| 35 | Cited but Not Verified: Parsing and Evaluating Source Attribution in LLM Deep Research Agents | [arXiv](https://arxiv.org/abs/2605.06635v1)<br>[PDF](https://arxiv.org/pdf/2605.06635v1)<br>[HTML](https://arxiv.org/html/2605.06635v1) | 问题：深度研究类 LLM 代理会生成带大量网页引用的 Markdown 报告，但现有评测常停留在引用是否存在、是否匹配或模型自报是否可信，缺少端到端检查：引用链接是否能访问、页面是否相关、页面内容是否真的支持被引用的句子。这个缺口会让用户把表面可验证的引用误当成事实可靠性。<br>方法：作者搭了一个三阶段评测框架：先让深度研究代理生成带内联引用的 Markdown 报告；再用确定性的 Markdown AST parser 抽取 citation-claim pair，支持编号引用、脚注、内联链接、autolink 和范围引用，并做句子级归因；最后抓取被引用网页，对每个引用从 Link Works、Relevant Content、Fact Check 三个维度打分。Link Works 是可访问性检查，Relevant Content 和 Fact Check 由 rubric-based LLM-as-a-judge 完成，并用 50-100 个人工检查样本做校准。实验覆盖 14 个闭源和开源模型、130 个来自 DeepResearch Bench 和 BrowseComp 的研究查询，还对 GPT-5.4 和 Claude Opus 4.6 做了 2 到 150 次 tool calls 的搜索深度消融。<br>实验：实验覆盖面不错：14 个模型、130 个查询、三类指标，并报告了完整表格和搜索深度消融。关键结果有辨识度：多数 frontier 模型 Link Works 超过 94%、Relevant Content 超过 80%，但 Fact Check 只有 39%-77%；开源模型一半以下能在 one-shot setting 里稳定产出带引用报告；搜索深度从 2 增到 150 次 tool calls 时，GPT-5.4 的 Fact Check 从 78.6% 降到 16.7%，Claude Opus 4.6 从 80.0% 降到 57.9%，而 Link Works 和 Relevant Content 基本保持高位。这些结果支撑了“表面引用质量掩盖事实失败”和“更多搜索可能造成事实综合过载”的主张。不过论文没有充分展开人工标注规模、judge 一致性、查询分布细节和网页内容抓取失败对结果的敏感性，严格性还有提升空间。<br>不足：主要局限有四点：第一，Relevant Content 和 Fact Check 依赖 LLM-as-a-judge，即使做了人工校准，也可能保留位置偏差、自我增强偏差或对长网页上下文的误判；第二，网页引用天然会随时间失效或内容变更，复现实验需要固定抓取快照，但论文当前更多是 live web 评测；第三，实验只覆盖有 web search 能力的公开模型，不覆盖企业 RAG 或私有知识库场景；第四，论文声称是 first end-to-end framework，表述偏强，和已有 attribution/citation verification 工作的边界需要读者自己再核对。 | 这篇论文把深度研究代理的引用质量拆成链接可访问、内容相关和事实支持三层来评测，最有价值的结论是：链接能打开、页面也相关，并不代表引用真的支撑了具体说法。 | 适合做 LLM agent 评测 / RAG/检索增强 / 引用生成 / 事实核查 | 值得读 |
| 36 | Detecting Time Series Anomalies Like an Expert: A Multi-Agent LLM Framework with Specialized Analyzers | [arXiv](https://arxiv.org/abs/2605.05725v1)<br>[PDF](https://arxiv.org/pdf/2605.05725v1)<br>[HTML](https://arxiv.org/html/2605.05725v1) | 问题：现有时间序列异常检测方法多输出分数或区间，解释和异常类型诊断弱；已有 LLM/VLM 方法又常让单个通用模型直接判断异常位置，面对长数值序列、变化点、周期破坏、形态变化等复杂模式时可控性和可靠性不足。<br>方法：SAGE保留原始序列给数值工具，同时给LLM一份压缩摘要；四个专门 analyzer 分别处理 point、structural、seasonal、pattern 异常，调用滚动统计、离群检测、STL、CUSUM、ACF/FFT/小波/STFT、SAX、recurrence plot、GAF/MTF等工具生成候选和证据。Detector按证据强度、跨 analyzer 一致性和冲突情况给候选区间打0-100分并归一化为置信度，同时结合从正常训练片段合成的ICL对照样例辅助类型排序。Supervisor再把结构化检测结果转成面向分析师的诊断、告警等级和建议。<br>实验：实验较扎实。主实验覆盖Yahoo S5、KPI、WSD三个TSAD基准，比较传统ML/DL方法以及SigLLM、LLMAD、TAMA、TSAD-Agents等LLM/VLM基线，并统一LLM/VLM骨干为Claude Sonnet 4.6。SAGE平均Point-F1/PA-F1/Affiliation-F1/Delayed-F1为0.663/0.851/0.910/0.849，均为最高；在KPI和WSD上相对LLMAD的Point-F1提升尤其明显。论文还做了合成类型评估、Yahoo子集结果、ICL零样本对比、backbone对比、组件消融和10名评估者25个样本的盲法人评，消融显示工具增强、多analyzer、视觉输入和合成ICL都有贡献。不过主结果对可产生分数的方法使用post-hoc Best-F1阈值搜索，作者也承认固定阈值性能大幅下降，因此部署性能不能直接按主表理解。<br>不足：只处理单变量时间序列，多变量关联异常留到未来工作；置信度更像序数证据分，不是校准概率，实际部署需要验证集或领域校准；Best-F1后验阈值使用测试标签估计可达性能，不是可直接上线的阈值策略；类型评估一部分依赖合成注入规则，真实benchmark类型标注只是单标注者的辅助分析；系统复杂度、LLM/API成本、prompt稳定性和工具参数敏感性也会影响实际落地。 | SAGE把单变量时间序列异常检测拆成点异常、结构异常、季节异常和模式异常四类 analyzer，再用证据汇总器输出区间、类型、置信度和诊断报告，实验结果强但部署阈值和单变量范围仍要谨慎看。 | 做AIOps / 工业监控 / 时间序列异常检测 / LLM工具调用/多agent系统 | 值得读 |
| 37 | From Articles to Premises: Building PrimeFacts, an Extraction Methodology and Resource for Fact-Checking Evidence | [arXiv](https://arxiv.org/abs/2605.06006v1)<br>[PDF](https://arxiv.org/pdf/2605.06006v1)<br>[HTML](https://arxiv.org/html/2605.06006v1) | 问题：事实核查文章里有大量证据、来源和推理，但通常只以长文形式存在；ClaimReview一类结构化标注覆盖了claim和verdict，却没有把支撑证据拆成机器可检索、可复用的细粒度单元。自动核查系统因此要么读整篇文章或整页来源，噪声大，要么缺少真实新闻场景下的证据数据。<br>方法：论文构建PrimeFacts：收集13,106篇英文PolitiFact文章、claim、verdict和来源信息，抽取49,718个文章内超链接作为证据锚点。方法分三种证据模式：A保留带外链的原句，B用LLM在全文和claim上下文中把锚点句改写成自包含premise并标注证据类型，C让LLM在不依赖锚点的情况下从全文开放抽取premise。评估包括BM25跨文章证据检索、六个指令LLM的零样本verdict预测，以及基于NLI和词重叠惩罚的DFS faithfulness指标，另有100条Mode B和100条Mode C的人审样本。<br>实验：实验规模和对照都比较扎实。检索上，Mode B相对原句A稳定提升，最好MRR@10从0.43到0.59；Mode C进一步到0.78-0.88。验证上，任何证据都明显超过多数类baseline，Mode B相对A通常带来4-7个二分类Macro-F1点，Mode C最高达到0.81二分类Macro-F1和0.42五分类Macro-F1。作者还报告了类别分布、六个模型、二分类和五分类设置、citation coverage、faithfulness自动指标和小规模人工评估。弱点是检索gold把同一篇fact-check的premise视作相关，和真实跨文章复用还有距离；Mode C的高分可能部分来自与claim更强的词面重合；人工评估样本只有200条，覆盖有限。<br>不足：依赖PolitiFact和英文新闻事实核查这一种平台/语言/写作风格，迁移到少链接文章、其他核查机构或多语言环境仍未验证。超链接锚点高精度但可能漏掉隐含证据，开放抽取又有冗余、claim词面贴近和LLM幻觉风险。方法抽出的是单条premise，没有显式建模事实核查文章中的多跳推理链和论证关系。数据因版权不释放原文，只释放衍生元数据和标注，这会影响复现实验和二次审计的便利性。 | PrimeFacts把PolitiFact长篇事实核查文章里的带链接证据改写成可复用的独立premise，实验证明这种中间层能明显提升证据检索和零样本判别表现。 | 自动事实核查 / NLP数据集构建 / 证据检索/RAG / 新闻可信度分析 | 值得读 |
| 38 | Inference-Time Budget Control for LLM Search Agents | [arXiv](https://arxiv.org/abs/2605.05701v1)<br>[PDF](https://arxiv.org/pdf/2605.05701v1)<br>[HTML](https://arxiv.org/html/2605.05701v1) | 问题：LLM search agent 在推理时既受工具调用次数限制，也受生成 token 限制；简单增加搜索、beam 或自我修正不一定更好，可能造成重复检索、过早回答、上下文膨胀或最终答案被不必要重写。论文关注的问题是：在每一步搜索时如何决定预算该花在检索、分解还是直接回答上，以及搜索结束后是否应该安全地替换最终答案。<br>方法：方法是训练无关的两阶段控制器，叠在 BAVT 风格树搜索骨架上。Stage 1 为 Search、Decompose、Answer 三类可行动作计算 task-level VOI 分数：先用 critic-derived progress、桥接/停滞/答案就绪等结构信号和预算压力惩罚形成效用，再按动作成本归一化，最后经过过早回答抑制、事实型问题分解抑制、组合问题最小搜索等确定性 guard 选择下一步动作。Stage 2 从完成轨迹中拿 base answer 和同源证据构造的 refined answer，只在 yes/no、二选一、typed slot、受证据支持的短事实补全等低风险场景替换，否则保留原答案；该阶段不新增工具调用和 LLM 调用。<br>实验：实验覆盖 HotpotQA、2WikiMultihopQA、MuSiQue、Bamboogle 四个多跳 QA 数据集，Qwen3-32B、Qwen3.5-122B、GPT-5.4-Mini 三个 backbone，以及四档双预算：(1,100)、(2,200)、(2,300)、(3,500)。比较对象包括 BAVT、BATS、AFlow、Search-o1，并使用相同 Search-R1 retrieval、question-only query、top_k=5 和严格 hard-budget audit，超出工具或输出 token 预算的样本计失败。Qwen3-32B 主表里 VOI 平均 EM/F1 为 0.35/0.42，高于 BAVT 0.28/0.34、BATS 0.24/0.30、AFlow 0.20/0.24、Search-o1 0.11/0.16；但它不是全面碾压，BATS 在部分高预算格子更强，AFlow 在 2Wiki lower-mid 领先。消融显示 Stage 1 是主要收益来源，移除预算惩罚损失最大；Stage 2 主要贡献局部答案格式修正。附录还给出跨 backbone macro delta、feasible-only usage 和运行时间探针，审计意识较强。<br>不足：方法依赖手工特征、固定系数和 guard，虽然作者声称跨数据集和 backbone 不调参，但泛化到更开放的网页浏览、更强检索器、长链任务或非 QA 任务仍未验证。收益在高预算和更强 backbone 下会变弱，Qwen3.5-122B 上呈混合结果，说明控制器可能主要吃低/中预算下的浪费空间。finalizer 只适合修 yes/no、二选一、slot、短事实补全等局部答案形式错误，无法补救错误检索路径或未解决桥接关系。实验报告了严格预算审计，但样本规模、代码可复现性和外部检索环境稳定性还需要进一步确认。 | 一篇面向 LLM 搜索代理的推理时预算控制论文，把下一步 search/decompose/answer 的选择做成显式 VOI 规则，并在严格工具调用和输出 token 双预算下做了较完整的多基准审计。 | 做 LLM agent / RAG/search agent / test-time compute / 成本受限推理 | 值得读 |
| 39 | Knowledge Graphs, the Missing Link in Agentic AI-based Formal Verification | [arXiv](https://arxiv.org/abs/2605.06434v1)<br>[PDF](https://arxiv.org/pdf/2605.06434v1)<br>[HTML](https://arxiv.org/html/2605.06434v1) | 问题：LLM 生成 SystemVerilog Assertions 时，常把自然语言规格和 RTL 当作松散文本处理，导致信号绑定不准、语义错配、语法或 elaboration 错误频繁出现；形式化工具反馈又常停留在一次性日志里，难以支持可追踪的迭代修复。<br>方法：流程先把规格、测试计划、RTL 元数据、属性、trace links、formal results、CEX、coverage metrics 等抽成 typed JSON IR，再导出 nodes.csv/edges.csv 构建运行时 KG。多 agent 围绕 KG 的局部邻域工作：property generation agents 生成并审查 SVA；syntax correction agents 根据编译诊断和上下文修复语法、宏和层级信号问题；CEX correction agents 解析 VCD 与失败点，区分 RTL bug、过强属性、缺失假设等；coverage improvement agents 根据覆盖缺口补充 cover/assertion。每次属性更新只失效相关下游证据，以便局部重验。<br>实验：实验覆盖 7 个开源/基准 RTL 设计，包括 FIFO、Lemming、ALU、CIC Decimator、BST、TTC Counter 和 CSR APB Interface，使用 GPT-5.2 与 Cadence JasperGold。表 II 给出了属性数量、vacuous properties、reachable RTL coverage、syntax fix attempts、CEX correction、end-to-end formal coverage 和 KG size。结果显示语法修复开销普遍很低，最终 formal coverage 约 82.98% 到 99.41%，CSR 的 reachable RTL coverage 从 33.8% 提升到 84.6%，FIFO 的通过属性数从 17/26 提升到 25/26。问题是缺少与无 KG、普通 RAG、已有断言生成系统的直接消融或统计显著性比较，且工具链依赖商用 JasperGold 和未公开细节，复现门槛较高。<br>不足：最大短板是语义修复能力仍不稳：BST 大量属性失败且 CEX 修复不收敛，Lemming 和 ALU 的剩余失败也没有明显改善，说明复杂 reset、FSM、多周期算术和全局数据结构不变量仍超出当前局部修补策略。论文没有清楚报告每个模块的人工介入、提示模板、运行成本、模型温度、失败样例细节，也没有充分消融 KG 中不同 artifact 的贡献。覆盖率提升还可能受 vacuity 和环境建模影响，CIC Decimator 与 BST 的 vacuous properties 提醒读者不要把高覆盖率直接等同于高质量验证。 | 把规格、RTL、生成的 SVA、形式化工具诊断、CEX 和覆盖率结果统一进可查询的知识图谱，再用多 agent 做断言生成和闭环修复；方向很实用，但实验还偏小、基线对比不够硬。 | 适合做 RTL/formal verification / SVA 自动生成 / EDA agent / LLM+工具闭环 | 值得读 |
| 40 | Knowledge-Graph Paths as Intermediate Supervision for Self-Evolving Search Agents | [arXiv](https://arxiv.org/abs/2605.05702v1)<br>[PDF](https://arxiv.org/pdf/2605.05702v1)<br>[HTML](https://arxiv.org/html/2605.05702v1) | 问题：Search Self-Play 这类自进化搜索智能体不用人工 QA 数据，但早期 Proposer 只从孤立答案实体造题，很多问题无效或不可验证；Solver 又只有答对/答错的二值奖励，检索到部分关键证据但最终答错的轨迹也被当成零分，训练信号很稀疏。<br>方法：方法沿用 SSP 的 Proposer-Solver 闭环，但先从 Wikidata 为每个种子实体离线抽取 LLM 引导的局部 KG 子图。目标路径的终点作为答案，整条子图作为 Proposer 造题上下文，路径中间节点作为 Solver 的近似 waypoint。核心奖励是 Waypoint Coverage Reward：正确答案仍给满分；错误但格式有效的轨迹，如果 think 文本精确覆盖了更多路径中间实体，就按组内归一化覆盖率给 alpha=0.3 的部分 credit，再用 GRPO 更新 Solver。Proposer 仍用 SSP 的困难度奖励，不直接吃 WCR。<br>实验：实验比较扎实。主实验覆盖 7 个 QA benchmark，包括 NQ、TriviaQA、PopQA 以及 HotpotQA、2Wiki、MuSiQue、Bamboogle 等多跳任务；报告了 6 个主配置，并在附录补到 9 个模型配置，包括 Qwen2.5/Qwen3/LLaMA/Search-R1/ZeroSearch 和 14B、32B 扩展。相对 SSP，所有配置平均分都有提升，弱初始化和多跳任务收益最大，例如 Qwen2.5-7B-Base 平均分从 44.9 到 49.4，多跳均值从 34.2 到 41.5；Qwen2.5-32B-Instruct 从 55.2 到 58.5。论文还做了 answer distribution 控制，说明只换 KG 终点答案并不能带来收益；做了 KG construction 与 WCR 的增量消融；用 EM/F1 复核主趋势；给出训练动态、coverage-correctness 相关性、生成 QA 有效率和计算成本。主要弱点是主指标依赖 DeepSeek-V3.2 LLM-as-a-judge，重复实验只在部分消融里看到，且没有把方法真正接入 Dr.Zero，只做额外横向比较。<br>不足：适用范围目前很窄：知识源是 Wikidata，任务是事实型多跳 QA，迁移到开放研究、claim verification、代码依赖图或领域 ontology 还没有验证。子图抽取依赖外部 LLM 做关系选择，路径质量受 selector 能力影响，而且需要处理完整 Wikidata dump，预处理成本不低。WCR 的 waypoint 匹配是实体标题精确子串覆盖，信号只是和正确性弱正相关，可能混入轨迹长度、搜索强度、实体别名等因素。部分单跳 benchmark 有回退，说明 KG 路径训练可能把分布推向多跳关系型问题。 | 把构造多跳问题时用到的 KG 路径复用成训练信号：一边给 Proposer 关系上下文，一边用路径中间实体给 Solver 的失败轨迹发部分奖励，明显改善 self-play 搜索智能体的多跳 QA 表现。 | LLM search agent / RAG agent 研究者 / 自进化训练与 RLHF/RLAIF 工程团队 / 多跳 QA 和知识图谱增强训练方向读者 / 如果只关心通用聊天模型或非事实型开放任务 | 值得读 |
| 41 | MemReranker: Reasoning-Aware Reranking for Agent Memory Retrieval | [arXiv](https://arxiv.org/abs/2605.06132v1)<br>[PDF](https://arxiv.org/pdf/2605.06132v1)<br>[HTML](https://arxiv.org/html/2605.06132v1) | 问题：Agent memory 系统常用 retrieve-then-rerank，但通用 reranker 往往只抓语义相似度，返回的片段看起来相关却不含回答所需信息；在生产里还会出现分数挤在低区间、阈值难设、时间/因果/多跳问题排序变差、不能利用对话上下文消歧等问题。<br>方法：模型以 Qwen3-Reranker 的 0.6B/4B 版本为底座，用最后 token 表征接线性头和 sigmoid 输出 0 到 1 的相关性分数。训练分几步：先用公开蒸馏数据保留通用排序能力；再用 GPT 与 Qwen 教师做成对比较、多数投票，并经 Elo/Bradley-Terry 转成连续软标签；随后在约 1M 通用 query-document 对和约 50K 多轮合成对话样本上做 BCE 点式蒸馏；最后用 InfoNCE 对中等相关度的难样本做 listwise/contrastive 微调。数据和指令设计特别覆盖意图聚焦、实体/关键词扩展、方面约束、时间约束、因果推理和指代消解。<br>实验：实验覆盖较宽。记忆场景用了 LOCOMO 和 LongMemEval，并统一用 BGE-M3 Top-100/Top-50 作为候选集，比较 BGE-Reranker、Qwen3-Reranker 0.6B/4B/8B、GPT-4o-mini、Gemini-3-Flash。LOCOMO 上 MemReranker-0.6B MAP 0.7150，接近 GPT-4o-mini 0.7151，4B MAP 0.7366，低于 Gemini-3-Flash 0.7772 但优于专用 reranker；LongMemEval 上 0.6B/4B MAP 分别为 0.7538/0.8043，高于 Gemini-3-Flash 0.7259。还做了 Opus-4.6 生成的 hard-case 评估、金融 FinFact、医疗 NFCorpus/SciFact/CMedQAv2 和延迟对比，0.6B 平均约 247ms，明显快于 GPT-4o-mini。不足是缺少清晰消融表来拆分 BCE、InfoNCE、Elo 标定、指令数据各自贡献；hard-case 是合成生成，可能贴近教师偏好；LongMemEval 中 GPT-4o-mini 有 19/500 个 API 失败样本被跳过，比较不完全；没有真实线上 A/B 或端到端问答收益。<br>不足：训练和评测大量依赖教师模型与合成数据，容易把教师偏好和数据构造方式蒸馏进模型；公开基准可能存在数据污染或与训练数据重叠，作者也承认标准测试有泄漏风险；没有展示完整消融、校准曲线、阈值选择稳定性、跨语言/长上下文极限测试和线上用户流量验证；金融数据表现接近饱和，区分度有限；医疗 CMedQAv2 上 MemReranker 不如 BGE 和 Qwen3-8B，说明垂直域泛化并不总是稳定。 | 面向 agent 长期记忆检索的专用 reranker，把 Qwen3-Reranker 通过多教师蒸馏、Elo/Bradley-Terry 标定、BCE 点式学习和 InfoNCE 细调改造成更会处理时间、指代、因果和多跳检索的轻量模型。 | 做 agent memory / RAG 检索 / reranker 训练 / 企业知识库检索 | 值得读 |
| 42 | NeuroAgent: LLM Agents for Multimodal Neuroimaging Analysis and Research | [arXiv](https://arxiv.org/abs/2605.06584v1)<br>[PDF](https://arxiv.org/pdf/2605.06584v1)<br>[HTML](https://arxiv.org/html/2605.06584v1) | 问题：多模态神经影像研究从原始 sMRI、fMRI、dMRI/DTI、PET 到可分析数据，需要跨 FreeSurfer、FSL、ANTs、MRtrix3、Elastix 等工具处理文件格式、配准、质控、参数和失败恢复。传统 workflow 能减轻脚本编排，但面对不完整、多站点或异构数据时仍需要大量人工检查、调参、重跑和下游分析代码重写。<br>方法：NeuroAgent 采用分层多 agent 架构：Central Orchestrator 解析自然语言需求并构建依赖图，按 sMRI、fMRI、PET、dMRI 等模态分派给 specialized agents；执行层使用 Generate-Execute-Validate 闭环，生成 Python 调用既有神经影像工具，在沙箱里执行，读取报错日志后自动修复，并用输出目录结构、BIDS/路径/文件完整性等规则做验证。系统还包含上下文记忆、状态持久化、数据整合、自然语言统计分析、可视化和 HITL 介入界面。<br>实验：实验比普通 agent demo 扎实，但仍是第一版系统验证。下游 AD vs. CN 分类使用 ADNI 全阶段 1,470 名受试者，CN=1,000、AD=470；所有人有 sMRI 和 tabular，子集有 Tau-PET n=469、fMRI n=278、DTI n=620。分类采用 subject-level stratified 5-fold CV，对比 ResNet-18、DenseNet-121、EfficientNet-B0 和 agent ensemble；四模态 sMRI+Tau-PET+fMRI+clinical 的 ensemble 达到 accuracy 0.8878、F1 0.8197、AUC 0.9518、MCC 0.7389，高于单一 backbone。pipeline ablation 覆盖 18 个 intent prompts、33 个 preprocessing cases、8 个 data integration cases：intent parsing 最高 joint EM 100%，preprocessing all-pass 最高 72.7%，data integration all-pass 仅 37.5-50%。另外，论文展示了 1,601 名 CN/MCI/AD 结构 MRI 匹配 cohort 的 cortical-thickness 分析，以及 1,137 名 DTI cohort 的 FA/MD 统计分析，结果方向符合已知 AD/年龄相关神经影像规律。<br>不足：关键限制也写得比较清楚：pipeline benchmark 规模小，不足以证明长尾鲁棒性；preprocessing 约四分之一 case 仍需 retry 或 HITL，data integration 更弱；没有和手写/传统 preprocessing pipeline 做下游质量对照，因此不能确认自动化预处理是否保持或损害了影像质量；ADNI 采集相对标准化，不能直接外推到真实临床异构数据；下游只验证 AD 分类，未覆盖回归、分割等任务；单被试全流程预处理耗时 4-8 小时，另有 LLM API 开销；少见影像伪影和领域特定失败仍可能需要人工介入。 | 一个把 LLM agent 放到多模态神经影像预处理、统计分析和 AD 分类全流程里的系统型论文，工程野心很足，但当前可靠性和泛化证据还没到可以放心自动化的程度。 | 做 LLM agent for scientific workflows / 医学影像/神经影像 pipeline / ADNI 多模态建模 / AutoML/数据工程自动化 | 值得读 |
| 43 | STALE: Can LLM Agents Know When Their Memories Are No Longer Valid? | [arXiv](https://arxiv.org/abs/2605.06527v1)<br>[PDF](https://arxiv.org/pdf/2605.06527v1)<br>[HTML](https://arxiv.org/html/2605.06527v1) | 问题：现有长期记忆评测多在看静态事实能不能找回，较少评估 agent 在新证据出现后是否知道旧记忆已经不再适用。论文关注 implicit conflict：后续观察没有直接否定旧记忆，却通过语境、常识或状态迁移让旧记忆失效，例如受伤不直接否定骑车通勤，但会改变近期通勤建议。<br>方法：论文构建 STALE 基准：400 个专家验证的隐式冲突场景、1200 个查询，覆盖 100 多个日常主题，长上下文可到约 150K tokens。它区分 Type I 共指/同属性更新和 Type II 跨属性传播式失效，并用三类 probing 评估：State Resolution 检查模型是否显式识别旧状态过期，Premise Resistance 检查模型是否能拒绝带有旧前提的误导性问题，Implicit Policy Adaptation 检查模型能否在自然下游任务中主动应用新状态。论文还给出 CUPMem 原型，把记忆写入阶段改成结构化状态整合、旧状态归档和传播感知搜索，查询阶段只从被裁定过的当前状态中读出。<br>实验：实验比较系统性，覆盖闭源模型、开源模型、主流 memory framework 和作者的 CUPMem。主表显示即使最强的 Gemini-3.1-pro 总体也只有 55.2%，Qwen3.5-27B 为 31.3%，很多 memory framework 低于 10%；CUPMem 达到 68.0%，说明显式状态裁定确实有帮助。论文还分析了 LightMem：新证据在 SR/PR 的 top-20 检索中出现率为 77.5%，但旧证据仍经常排在前面，PR 中即使新证据可见仍有 99.0% 失败，支持“不是单纯检索问题”的论点。评估使用 LLM-as-judge，但附录给出 95.83% 人类一致率和较低 false positive rate，可信度比只报自动评测更高。<br>不足：基准是受控诊断环境，每个样本围绕单次隐式状态转移，不能覆盖真实交互中反复更新、多属性耦合和渐进漂移。数据由 LLM 生成后人工验证，日常场景覆盖广但仍可能和真实用户日志分布有差距，distractor 来自 LongMemEval 而非同一 persona。评估端依赖 LLM judge，虽然有人类一致率验证，但 IPA 这类开放式下游任务仍可能有细微误判。CUPMem 依赖预定义状态 schema，说明方向有价值，但距离通用、schema-free 的长期记忆系统还有距离。 | STALE 把长期记忆里的“旧记忆何时失效”单独拎出来做成基准，显示当前 LLM 和主流 memory framework 常能找回新证据，却很难把它变成稳定的当前状态判断。 | LLM agent 和长期记忆系统研究者 / 做个性化助手 / 用户画像更新 / memory/RAG 产品评测的人 | 值得读 |
| 44 | SkillRet: A Large-Scale Benchmark for Skill Retrieval in LLM Agents | [arXiv](https://arxiv.org/abs/2605.05726v1)<br>[PDF](https://arxiv.org/pdf/2605.05726v1)<br>[HTML](https://arxiv.org/html/2605.05726v1) | 问题：LLM agent 的可复用 skill 库变大后，不能再靠用户显式点名或把所有 skill 塞进上下文；系统需要从大量长文档 skill 中选出少量相关项。已有 skill benchmark 多看端到端执行，现有 skill routing 工作评测集小且训练数据不完整公开，难以单独衡量检索层能力。<br>方法：作者从 claude-plugins.dev 抓取 22,795 个公开 GitHub agent skills，经描述恢复、语言过滤、许可证过滤、内容去重和搜索目标去重后得到 17,810 个 skill。每个 skill 用 name|description|SKILL.md 作为检索文档，并用 LLM 标注出 6 个大类、18 个子类的两级 taxonomy。查询通过 self-instruct 式流程生成：采样 1-3 个 skill，用 GAIA 风格示例约束真实用户请求风格，训练查询由 Qwen3.5-122B-A10B 生成，评测查询由 Claude Opus 4.6 生成；之后做 3-gram 泄漏检测、多视角 LLM 审核和人工专家验证。评测覆盖 BM25、encoder-only embedding、decoder-only embedding、SkillRouter baseline、reranker，以及基于 SkillRet 训练集微调的 Qwen3 embedding/reranker。<br>实验：实验比较扎实。评测模型范围广，指标包含 NDCG、Recall 和 Completeness，考虑了 top-k reranking 深度、文档表示、模型最大上下文、MTEB 排名相关性、类别分层结果和 sentence erasure 分析。关键结果是最强 off-the-shelf retriever NDCG@10 约 0.665，SkillRouter-Embedding-0.6B 约 0.704，SkillRet-Embedding-8B 微调后到 0.835；SkillRet-Reranker-0.6B 对 0.6B embedding 还有增益，但对 8B embedding 增益变小。数据质量方面有人审，但人工验证样本相对总体规模仍偏小。<br>不足：最大限制是查询仍为合成数据，不来自真实 agent 使用日志，可能低估简短、上下文依赖、对话式、信息不足请求的难度。它只评估检索排序，不验证检索到的 skill 能否被正确选择、组合、解释和执行，所以高 NDCG 不等于端到端 agent 更可靠。数据源也偏公开 GitHub skill 生态，软件工程类占 62.2%，领域分布很不均衡；taxonomy 和查询生成都依赖强 LLM，可能带入模型风格和标注偏差。 | 一个针对 LLM agent skill 检索的较大规模 benchmark，数据构建、评测和开源资产都比较完整，适合做 agent 技能库检索/路由的基准和训练起点。 | Agent 系统工程师 / 做 tool/skill retrieval 的研究者 / embedding/reranker 训练团队 / 维护大规模 prompt/skill 库的平台团队 | 值得读 |
| 45 | Sheet as Token: A Graph-Enhanced Representation for Multi-Sheet Spreadsheet Understanding | [arXiv](https://arxiv.org/abs/2605.05811v1)<br>[PDF](https://arxiv.org/pdf/2605.05811v1)<br>[HTML](https://arxiv.org/html/2605.05811v1) | 问题：多 sheet 工作簿里的相关证据常分散在不同 tab 中。现有 spreadsheet RAG 往往按行、列、块或文本 chunk 检索，虽然可扩展，但容易打碎 worksheet 的整体语义，也弱化 sheet 之间的结构关系。论文把任务限定为给定自然语言 query，从候选 sheets 中找出支持后续 QA、推理或代码生成的 sheet set。<br>方法：方法分两阶段。第一阶段从每个 sheet 抽取 sparse schema-aware record，包括 workbook/tab 名、grid shape、最多 12 个列名、每列第一个非空示例值，然后线性化为文本，用 12 层预训练 Transformer 编成一个 dense Sheet Token，并用 pairwise matching 监督训练。第二阶段冻结 Sheet Encoder，针对 query 构造候选 sheet 图，边关系来自 semantic similarity、query-conditioned relevance、schema consistency、shape compatibility 等通道，再用多阶段 graph transformer/graph convolution 做关系组合和消息传递，最后通过 query-guided pooling 与 set-level/node-level 目标训练 listwise retrieval。<br>实验：实验能说明框架在自建 IndustryTab 上可以跑通，但说服力有限。数据集只有 614 sheets、1842 pairwise instances、134 listwise queries，且主要来自模板增强、Valentine fabricator 生成的配对与少量真实/翻译数据。主结果报告 Stage 2 eval accuracy 0.8438；附录消融显示 full model accuracy 84.4、shallow baseline 81.2、without column examples 68.8，说明 column examples 和增强图检索都有贡献。效率表报告 Stage 1 约 45.9 GFLOPs/pair、35-39 samples/s，Stage 2 约 235 GFLOPs/query、18.3 queries/s。主要问题是缺少强基线：没有与 chunk-based spreadsheet RAG、BM25/dense/RRF、long-context LLM、SheetCompressor/SheetBrain 类方法做端到端比较；指标也只看 accuracy 和 entropy，没有 recall@k、nDCG、MRR、set F1，也没有下游 QA/code generation 成果。没有多随机种子、显著性检验或跨真实工作簿泛化测试。<br>不足：论文自己也承认只用 sheet name、headers、representative values 和 shape，会漏掉具体单元格、公式、图表、格式、批注和嵌入对象。除此之外，数据集偏小且合成比例高，listwise query 的正例定义较规则，可能高估 schema/shape 信号的作用；sheet-level token 对需要细粒度 cell evidence 的任务可能召回不足；graph retriever 的候选集生成、阈值选择、真实部署延迟和与下游生成器的接口没有充分评估。 | 把 worksheet 当成检索基本单元，再用 query-specific graph 做跨 sheet 检索，思路适合 spreadsheet agent/RAG 方向跟踪，但实验规模和基线还撑不起强结论。 | 适合做 spreadsheet understanding / 表格 RAG / 数据分析 agent / 企业 workbook 检索 | 值得读 |
| 46 | GazeMind: A Gaze-Guided LLM Agent for Personalized Cognitive Load Assessment | [arXiv](https://arxiv.org/abs/2605.05790v1)<br>[PDF](https://arxiv.org/pdf/2605.05790v1)<br>[HTML](https://arxiv.org/html/2605.05790v1) | 问题：智能眼镜上的 AI 助手通常只能看外部场景，缺少对用户认知负荷的感知，因此难以判断什么时候该减少打扰、简化反馈或主动辅助。传统认知负荷测量要么依赖 EEG/fMRI 等不适合轻量眼镜的传感器，要么用眼动特征加传统分类器，但解释性弱、跨任务和跨用户泛化差。<br>方法：GazeMind 把过去几秒的眼动信号转成结构化表格特征，包括 fixation、saccade、blink、pupil 等指标；再用 Task-Guidance Reasoning 从训练统计中生成任务相关规则，告诉 LLM 不同任务里同一眼动模式如何解释；Adaptive User Profile Calibration 用短校准和聚类把用户分成 High-Reactor、Low-Reactor、Restless 等类型，并加入个人 pupil/blink baseline；CogRAG 用统计描述符检索同任务、同用户类型的历史样例及标签，最后把特征表、任务规则、用户画像和检索样例放进统一 LLM prompt 输出 low/moderate/high 认知负荷和解释。<br>实验：实验设计相对完整。作者用跨用户 70/30 切分，测试 46 个未见用户；比较 Decision Tree、SVM、MLP、TCN、Transformer、LSTM、Time-LLM、Llama-3.3-70B、GPT-4o 等基线，主结果为 GazeMind 62.73% accuracy / 62.11 F1，明显高于三十多到四十出头的基线。Reading 和 Gaming 子任务分别达到约 64.98/60.63 accuracy。消融显示 GPT-4o+TGE 约 39.62 accuracy，加入 TGR 到 45.34，加入 AUP 到 49.10，加入 CogRAG 到 62.73，说明主要增益来自检索样例，也有任务规则和用户校准贡献。附录还补了同等上下文 Transformer、公平性讨论、延迟、小模型 Qwen3.5-4B、标签噪声、缺失 gaze、跨任务规则迁移、解释 faithfulness 和数据集统计。<br>不足：主要限制是绝对性能仍不高，三分类 accuracy 约六成，离可靠产品化还有距离；标签来自 15-30 秒一次的主观自报告，并经过 7 类到 3 类聚合和时间回填，噪声不可避免；TGR 虽不微调 LLM，但仍要从目标任务训练统计里离线生成规则，新任务零样本能力没有完全解决；任务覆盖只有 reading、social game、audio calibration，作者也承认还需要 driving、cooking 等更复杂日常场景；方法目前只用 gaze，未真正利用视频/音频上下文；数据和代码可用性在 HTML 正文中没有看到明确发布链接。 | 一篇面向智能眼镜的 gaze-based cognitive-load 论文，真正有价值的是把眼动特征、任务规则、用户校准和检索样例组织成 LLM 推理流程，并配套了一个较大的 CogLoad-Bench 数据集。 | HCI / AR smart glasses 研究者 / 眼动与认知负荷建模研究者 / 做可穿戴个性化 AI 助手的人 / 研究 LLM for time-series reasoning 或 physiological signal reasoning 的人。 | 值得读 |
| 47 | Pest-Thinker: Learning to Think and Reason like Entomologists via Reinforcement Learning | [arXiv](https://arxiv.org/abs/2605.06121v1)<br>[PDF](https://arxiv.org/pdf/2605.06121v1)<br>[HTML](https://arxiv.org/html/2605.06121v1) | 问题：通用多模态大模型直接做农业害虫识别时，很难稳定抓住眼、胸、翅、足、体表斑纹等细粒度形态特征；害虫又有近缘种相似、同种不同发育阶段差异大、专家标注稀缺等问题，导致普通 SFT 或通用视觉推理模型在真实农业场景里泛化不稳。<br>方法：作者构建了两个高清害虫基准 QFSD 和 AgriInsect，分别包含 7054 张/141 类和 9452 张/200 类图像，并请昆虫学专家按 Head、Thorax、Abdomen、Wing、Leg、Body Surface、Special Markings 等部位整理形态知识。训练分两步：先用 Qwen2.5-VL-72B-Instruct 结合专家知识生成 QFSD-CoT-2K 和 AgriInsect-CoT-3K，用于冷启动 SFT，让模型学会带 `<think>` 的形态推理轨迹；再用 QFSD-RL-4K 和 AgriInsect-RL-6K 做 GRPO 强化学习。奖励由格式奖励、答案准确奖励和 feature reward 组成，其中 feature reward 用 Qwen3-235B-A22B-Instruct 作为 LLM-as-a-Judge，对回答中是否基于可观察形态特征推理打 0-10 分。<br>实验：实验比较完整。主实验覆盖 QFSD 和 AgriInsect 两个基准，并与 GPT-5、GPT-5-mini、Gemini-2.5-Flash、InternVL3/3.5、Qwen2.5VL/Qwen3VL、Qwen3VL-Thinking、VisionThink、Vision-SR1、Vision-G1 等模型比较。Pest-Thinker-7B 在 QFSD overall 达到 84.32%，接近或超过多个强基线；在 AgriInsect overall 达到 82.77%，明显高于 Qwen2.5VL-7B 的 49.48% 和 Qwen2.5VL-72B 的 59.06%。few-shot 表明 RL 在 1/2/4/8-shot 下比 SFT 稳定带来增益；消融表明仅 RL 或仅格式+准确奖励不够，加入 feature reward 后 QFSD overall 从 67.64% 提到 75.96%，AgriInsect 也有明显提升。还做了 24 个同种不同状态互斥测试的泛化实验，Pest-Thinker-7B overall 为 70.55%，优于 Qwen2.5VL-7B 的 52.86%。不足是评价主要仍是分类准确率和作者自建数据集，真实田间部署、跨地区/跨设备鲁棒性、专家人工评估和统计显著性不够充分。<br>不足：数据集和代码只写了 upon acceptance，当前可复现性有限；feature reward 依赖另一个大模型做 judge，可能把 judge 偏好蒸馏进策略，且评分提示细节只说在补充材料；任务主要验证害虫类别识别和形态描述，没有覆盖真实农业监测中的低清、遮挡、复杂背景、幼虫/成虫混杂、开放集新物种和成本约束；方法需要专家知识整理，迁移到其他农业对象时仍有人工知识工程成本。 | 一篇把昆虫形态学知识、CoT 冷启动和 GRPO 奖励结合起来做害虫细粒度识别的应用型 MLLM 论文，实验增益明显，但新意主要在领域适配和奖励设计，不是通用方法突破。 | 农业智能监测研究者 / 细粒度视觉识别研究者 / 多模态后训练/RLHF/RLAIF 工程师 / 需要把专家知识注入 MLLM 的垂直行业团队。 | 值得读 |
| 48 | DINORANKCLIP: DINOv3 Distillation and Injection for Vision-Language Pretraining with High-Order Ranking Consistency | [arXiv](https://arxiv.org/abs/2605.06592v1)<br>[PDF](https://arxiv.org/pdf/2605.06592v1)<br>[HTML](https://arxiv.org/html/2605.06592v1) | 问题：CLIP 式图文预训练有两个被反复指出的问题：InfoNCE 把 batch 内所有非匹配对粗暴当负样本，丢掉非匹配样本之间的相对排序；视觉侧全局池化又会压掉 patch/部件级局部结构。RankCLIP 只补了排序一致性，而且是一阶模型，不能表达“已选邻居组合会影响下一个候选”的高阶关系。<br>方法：方法由两块组成。第一块是 DINOv3 teacher injection：冻结 DINOv3-ViT-L，预提取 teacher 特征并用 PCA 降到 256 维；训练轻量 ViT-Tiny 图像/文本 student，用 patch Gram 矩阵损失和 relational-distance loss 保留 teacher 的 dense 结构；再通过 1D SPP、channel-spatial attention、self-attention refiner 和 conflict-aware gate，把 student residual 加到 CLIP embedding 上。第二块是高阶 Plackett-Luce ranking loss：在 RankCLIP 的候选 utility 外加入 pairwise beta 和 tuple-wise gamma transition terms，默认 R=3，并用 row-centering、gate regularization 和 warm-start 控制训练。<br>实验：设计上覆盖面很宽：CC3M 2.71M 有效图文对、ViT-B/32、CLIP/CyCLIP/ALIP/RankCLIP matched compute baselines，报告 ImageNet1K zero-shot、MSCOCO retrieval、ImageNetV2/ImageNet-R、10 个 linear probing 数据集、5 个 fine-grained probe、order sweep、组件消融、teacher ablation、fusion ablation、成本分析和 qualitative cases。主要问题也很大：实验章节明确写道所有红色数字是根据 RankCLIP/CLIP on CC3M 趋势 projected，待 run complete 后替换；HTML 文本中表格数字无法可靠区分哪些是最终实测、哪些是投影。因此目前只能把实验当作实验计划加部分趋势展示，不能当成完整实证支撑。另一个弱点是整体精度很低，例如 CC3M 从零训练的 ImageNet1K top-1 约 9-11%，说明设置偏小，实际应用说服力有限。<br>不足：核心 limitation 是结果可信度：论文自称存在 projected numbers，代码也只是承诺 accepted 后公开。方法上，高阶 ranking head 对 warm-start schedule 敏感；R>3 开始饱和或下降，规模变大时最优阶数可能变化；当 caption 只描述全局类别、没有局部细节时 gate 关闭，退化得接近 RankCLIP；teacher 的 text-aligned projection 在欠代表领域可能和 contrastive text encoder 不对齐，文本分支会引入噪声。训练还依赖 DINOv3 teacher feature cache 和 H100 资源，虽然作者说 72 小时内完成，但复现门槛仍不低。 | 把 RankCLIP 的一阶 Plackett-Luce 排序一致性扩到高阶，再把 DINOv3 的 dense 局部结构蒸馏后注入 CLIP 主干；想法有意思，但论文自己承认实验数字包含 projected results，证据还不够硬。 | 做视觉语言预训练 / CLIP loss 改造 / 细粒度识别 / DINO/DINOv2/DINOv3 feature distillation | 略读 |
| 49 | Retrieval-Conditioned Topology Selection with Provable Budget Conservation for Multi-Agent Code Generation | [arXiv](https://arxiv.org/abs/2605.05657v1)<br>[PDF](https://arxiv.org/pdf/2605.05657v1)<br>[HTML](https://arxiv.org/html/2605.05657v1) | 问题：多 agent 代码生成系统常常先按 query 文本或固定规则决定用单 agent、子 agent、流水线还是更重的 research-style 拓扑，但真正决定任务复杂度的往往是待修改代码的结构：涉及多少文件、依赖链多深、跨模块耦合多强。论文要解决的是：在选择 orchestration topology 前先看代码库结构，同时保证动态选出的 agent DAG 不会突破父级资源预算。<br>方法：作者提出 Retrieval-Guided Adaptive Orchestration (RGAO)，放在 Code-Agent 框架里运行。系统先用层级代码索引和多策略检索得到相关代码上下文，再从检索子图中抽取 5 维复杂度向量：最大依赖深度、涉及文件数、符号数、树遍历深度、跨模块耦合比例。路由器结合 query 和这个向量选择 FastPath、SubAgent、MultiAgent、DeepResearch 四类拓扑。另一条线是 contract-based sub-agent abstraction：每个 agent 带输入、能力、工具和内存约束，并有 6 维预算向量；论文定义预算组合代数和 VerifyConservation，在执行任何 LLM 调用前用 O(|V|+|E|) 检查选出的 delegation forest 是否守住根预算。<br>实验：实验设计能支持“路由器和预算检查开销很低”以及“在作者构造的路由标签集上比 regex 路由更准”，但不能充分支持“能提升真实多 agent 代码生成”。路由评测用 250 个标注样本，100 个调阈值、150 个评测，三名有代码生成经验的标注者，Fleiss kappa=0.78；RGAO 将 misrouting 从 regex baseline 的 30.1% 降到 8.2%，McNemar p<1e-6。微基准显示 20-task DAG 构建 <0.01 ms，200 文件/2002 节点 tree index 构建 11.1±0.4 ms，contract factory 1.10±0.03 us，预算检查 0.65±0.02 us，路由决策 0.11±0.02 ms，4-path RRF 检索 0.90±0.08 ms。另有 5 个 compound pipeline proxy，报告 100% pipeline correctness、约 6000 tokens/pipeline；10 个 synthetic SWE-bench issue 上 pass@1 为 40%。但这些都是 proxy harness 或 synthetic set，论文明确没有跑完整 SWE-bench Verified / SWE-bench Pro，外部有效性有限。<br>不足：最大短板是实证部分偏小、偏代理任务。完整 SWE-bench Verified 和 SWE-bench Pro 被推迟到后续，作者解释为污染问题和约 220 GPU-hours 成本，但这意味着论文还没有证明 RGAO 在真实长程代码修复上优于强 baseline。路由器是手调阈值，不是学习器，250 个样本也容易过拟合到作者定义的拓扑标签。预算守恒证明依赖确定性工具成本、有界检索深度和有限动作空间；一旦 LLM sampling、工具运行时间、外部 API 和失败重试有随机性，就退化为期望预算或需要额外 tail bound。复杂度向量也很轻，只含依赖深度、文件数、符号数、树深度和跨模块耦合，作者承认没有覆盖圈复杂度、测试覆盖密度、语言特定结构等重要信号。另一个现实问题是，论文声称结合了 LATTICE、KohakuRAG、RepoGraph 等思想，但关键收益主要来自 proxy 标签集，尚缺跨语言、跨仓库和真实用户任务的独立复验。 | 这篇把代码检索得到的结构复杂度用于多 agent 拓扑选择，并给预算守恒形式化证明；思路值得扫读，但实验主要是小规模 proxy harness，离真实代码生成收益还有距离。 | 适合做 multi-agent coding system / IDE agent / agent orchestration / 代码检索 | 略读 |
| 50 | X-OmniClaw Technical Report: A Unified Mobile Agent for Multimodal Understanding and Interaction | [arXiv](https://arxiv.org/abs/2605.05765v1)<br>[PDF](https://arxiv.org/pdf/2605.05765v1)<br>[HTML](https://arxiv.org/html/2605.05765v1) | 问题：现有移动 agent 往往依赖截图式 GUI 操作、云手机或单一交互入口，难以同时利用手机屏幕、摄像头、语音、个人数据和真实 Android app 状态；同时长链路操作容易受广告、页面漂移、跨 app 状态变化影响，用户也缺少可定制、可复用的执行路径。<br>方法：X-OmniClaw 把系统拆成 Omni Perception、Omni Memory、Omni Action 三个紧耦合模块。感知侧统一接入 UI、摄像头、语音、定时任务和外部网关，用时间对齐与 VLM 将多模态输入转成结构化意图；记忆侧维护运行时工作记忆，并从本地相册、交互轨迹和任务元数据中提炼长期个人记忆，辅以过滤、脱敏和开关控制；动作侧结合 Android XML、OCR、视觉 grounding、技能和工具执行，并通过 dumpsys activity、deeplink 和 Intent 参数把用户导航克隆成可复用技能，后续尽量直接跳转目标页面而不是逐步重放点击。<br>实验：weak: 论文展示了三类 demo，包括摄像头识别后进入购物/本地服务结果页、ScreenAvatar 处理屏幕投影任务、基于相册记忆一键生成视频、克隆美团闪购页面入口等；但没有报告成功率、延迟、错误类型、用户研究、对比系统、消融实验或 AndroidWorld/OSWorld 等公开基准结果。图示能说明流程，不能证明鲁棒性或泛化能力。<br>不足：主要限制是证据不足：没有量化评测，也没有说明测试设备、应用版本、任务数量、失败案例和隐私安全边界。系统依赖 Android introspection、deeplink/Intent、OCR/VLM 和云端 LLM 推理，遇到应用权限限制、未导出 Activity、界面大幅改版、反自动化机制、敏感数据上传和本地模型能力不足时可能很脆弱。长期记忆虽然提到过滤和用户开关，但缺少具体策略和安全评估。 | 一篇面向 Android 的移动端多模态 agent 架构报告，值得了解工程思路，但缺少量化实验和可复现评测，研究增量有限。 | 移动端 agent 工程师 / Android 自动化开发者 / 个人助理产品/系统架构研究者 / 关注 GUI agent 落地而非纯模型指标的人。 | 略读 |
